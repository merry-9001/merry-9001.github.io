<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://merry-9001.github.io</id>
    <title>看风听歌</title>
    <updated>2019-12-24T11:22:36.266Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://merry-9001.github.io"/>
    <link rel="self" href="https://merry-9001.github.io/atom.xml"/>
    <subtitle>学习是一辈子的事</subtitle>
    <logo>https://merry-9001.github.io/images/avatar.png</logo>
    <icon>https://merry-9001.github.io/favicon.ico</icon>
    <rights>All rights reserved 2019, 看风听歌</rights>
    <entry>
        <title type="html"><![CDATA[tp5]]></title>
        <id>https://merry-9001.github.io/post/tp5</id>
        <link href="https://merry-9001.github.io/post/tp5">
        </link>
        <updated>2019-12-24T11:22:08.000Z</updated>
        <content type="html"><![CDATA[<h1 id="tp5">tp5</h1>
<h2 id="路径">路径</h2>
<p>http://localhost:8000/library/public/admin/users/index.html</p>
<pre><code>admin/users/index.html
</code></pre>
<p>admin下的控制器里面的User.php里面的方法名</p>
<pre><code>public function index()
{

		$selectResult=Db::table('book_user')//热点精选
			-&gt;join('book_role','book_user.role_id = 				book_role.role_id')
		    -&gt;select();
		$this-&gt;assign('user',$selectResult);
	//	return dump($selectResult);
   return $this-&gt;fetch('index');
}
</code></pre>
<h2 id="前台传入后台值通过form">前台传入后台值（通过form）</h2>
<form class="form-auth-small" method="post" action="{:Url('admin/index/login')}">
<pre><code>&lt;form class=&quot;form-auth-small&quot; method=&quot;post&quot; action={:Url('admin/index/login')}&quot;&gt;
</code></pre>
<pre><code>if(request()-&gt;isPost())

</code></pre>
<p>控制器用来判断是否是form表单提交过来的请求 如果是的话会走if</p>
<h2 id="返回前台代码">返回前台代码</h2>
<pre><code> return $this-&gt;fetch('login');
</code></pre>
<h2 id="获取表单数据">获取表单数据</h2>
<pre><code> $data = input(&quot;post.&quot;);
</code></pre>
<p>这样是获取到所有数据</p>
<pre><code>// return dump($data[&quot;name&quot;]);
</code></pre>
<p>这样可以获取到每一条的数据</p>
<h2 id="调试打印">调试打印</h2>
<hr>
<pre><code>    return dump($data);
</code></pre>
<h2 id="database">database</h2>
<p>application的下database用来操作数据库</p>
<pre><code>'type'            =&gt; 'mysql',
// 服务器地址
'hostname'        =&gt; 'usxjsj.cn',
// 数据库名
'database'        =&gt; 'db16147230',
// 用户名
'username'        =&gt; 'u16147230',
// 密码
'password'        =&gt; 'xg1511',
</code></pre>
<h2 id="数据库操作">数据库操作</h2>
<h3 id="增">增</h3>
<pre><code>				   if($data['type1']=='学生')
				   {

					Db::name(&quot;book_admin&quot;)-&gt;insert([
						'admin_account'=&gt;$data['name'],
						'admin_password'=&gt;$data['password'], 
						]
					);
</code></pre>
<p>name(表名)</p>
<p>'admin_account'=&gt;$data['name'],</p>
<p>表里面的字段名=&gt;插入的名字</p>
<h3 id="改">改</h3>
<pre><code>$res` `= Db::name(``'style'``)-&gt;where(``'id'``,4)-&gt;update([``'name_cn'``=&gt;``'李四'``]);
</code></pre>
<p>where(数据库字段名,（值）)逗号表示等于</p>
<h3 id="查">查</h3>
<pre><code>				$s=Db::table('book_borrow')//热点精选
				-&gt;where('book_state',1)
				 -&gt;select();
</code></pre>
<p>$s拿到的就是叫table的表名里面的数据 ,where里面写条件</p>
<pre><code>			$s=Db::table('book_borrow')//热点精选
			-&gt;join('book_info','book_book.book_id = book_info.book_id')
			//-&gt;whereTime('drop_litter_time', 'month')
			-&gt;where('book_state',1)
			 -&gt;select();
</code></pre>
<p>$s拿到的就是叫table的表名里面的数据 ,where里面写条件,join用来连接表，要查两张表里面的接起来的内容</p>
<h2 id=""></h2>
<h3 id="删">删</h3>
<pre><code>$res = Db::name('style')-&gt;where('id',2)-&gt;delete();
</code></pre>
<p>按照条件删除</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[正则表达式]]></title>
        <id>https://merry-9001.github.io/post/zheng-ze-biao-da-shi</id>
        <link href="https://merry-9001.github.io/post/zheng-ze-biao-da-shi">
        </link>
        <updated>2019-12-23T08:29:10.000Z</updated>
        <content type="html"><![CDATA[<h1 id="正则表达式">正则表达式</h1>
<h2 id="量词">量词</h2>
<p>通过量词可以设置一个内容出现的次数</p>
<p>量词只对它前边的一个内容起作用</p>
<pre><code>  {n} 正好出现n次

  {m,n} 出现m-n次

  {m,} m次以上

  + 至少一个，相当于{1,}

  * 0个或多个，相当于{0,}

  ? 0个或1个，相当于{0,1}
</code></pre>
<h2 id="开头结尾">开头结尾</h2>
<p>^ 表示开头</p>
<p>$ 表示结尾</p>
<p>如果在正则表达式中同时使用^ $则要求字符串必须完全符合正则表达式</p>
<pre><code>reg = /^a$/;
</code></pre>
<h2 id="手机号检测">手机号检测</h2>
<p>检查一个字符串是否是一个合法手机号</p>
<pre><code>var phoneReg = /^1[3-9][0-9]{9}$/;

console.log(phoneReg.test(phoneStr));
</code></pre>
<p>以1开头</p>
<p>第二位3-9任意数字</p>
<p>三位以后任意数字9个</p>
<h2 id="通配">通配</h2>
<pre><code>\w
任意字母、数字、_  [A-z0-9_]
\W
除了字母、数字、_  [^A-z0-9_]
\d
任意的数字 [0-9]
\D
除了数字 [^0-9]
\s
空格
\S
除了空格
\b
单词边界
\B
除了单词边界
</code></pre>
<h2 id="邮件正则">邮件正则</h2>
<pre><code>var emailReg = /^\w{3,}(\.\w+)*@[A-z0-9]+(\.[A-z]{2,5}){1,2}$/;

var email = &quot;739.211562@qq.com&quot;;
</code></pre>
<p>任意字母数字下划线    .任意字母数字下划线  @   任意字母数字     .任意字母（2-5位）   .任意字母（2-5位）</p>
<pre><code>\w{3,}  (\.\w+)*  @  [A-z0-9]+  (\.[A-z]{2,5}){1,2}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[markdown标记语言]]></title>
        <id>https://merry-9001.github.io/post/markdown-biao-ji-yu-yan</id>
        <link href="https://merry-9001.github.io/post/markdown-biao-ji-yu-yan">
        </link>
        <updated>2019-11-08T13:28:47.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li>HTML 也是标记语言</li>
<li>markdown 标记语言</li>
</ul>
<p>优点：<br>
1、因为是纯文本，所以只要支持Markdown的地方都能获得一样的编辑效果，可以让作者摆脱排版的困扰，专心写作。<br>
2、操作简单。比如:WYSIWYG编辑时标记个标题，先选中内容，再点击导航栏的标题按钮，选择几级标题。要三个步骤。而Markdown只需要在标题内容前加#即可</p>
<p>缺点：<br>
1、需要记一些语法（当然，是很简单。五分钟学会）。<br>
2、有些平台不支持Markdown编辑模式。</p>
<h1 id="一-标题">一、标题</h1>
<pre><code># 这是一级标题
## 这是二级标题
### 这是三级标题
#### 这是四级标题
##### 这是五级标题
###### 这是六级标题
</code></pre>
<h1 id="二-字体">二、字体</h1>
<pre><code>**这是加粗的文字**
*这是倾斜的文字*`
***这是斜体加粗的文字***
~~这是加删除线的文字
</code></pre>
<h1 id="三-引用">三、引用</h1>
<blockquote>
<p>这是引用的内容</p>
</blockquote>
<pre><code>&gt;这是引用的内容
</code></pre>
<h1 id="四-分割线">四、分割线</h1>
<hr>
<pre><code>---
</code></pre>
<h1 id="五-超链接">五、超链接</h1>
<p><a href="http://baidu.com">百度</a></p>
<pre><code>[百度](http://baidu.com)
</code></pre>
<h1 id="六-列表">六、列表</h1>
<ul>
<li>
<p>无序列表</p>
<p>-列表内容<br>
+列表内容<br>
*列表内容<br>
注意：- + * 跟内容之间都要有一个空格</p>
</li>
<li>
<p>有序列表</p>
<p>1.列表内容<br>
2.列表内容<br>
3.列表内容<br>
注意：序号跟内容之间要有空格</p>
</li>
</ul>
<h1 id="七-表格">七、表格</h1>
<pre><code>姓名|技能|排行
--|:--:|--:
刘备|哭|大哥
关羽|打|二哥
张飞|骂|三弟

表头|表头|表头
---|:--:|---:
内容|内容|内容
内容|内容|内容

第二行分割表头和内容。
有一个就行，为了对齐，多加了几个
文字默认居左
-两边加：表示文字居中
-右边加：表示文字居右
</code></pre>
<table>
<thead>
<tr>
<th>姓名</th>
<th style="text-align:center">技能</th>
<th style="text-align:right">排行</th>
</tr>
</thead>
<tbody>
<tr>
<td>刘备</td>
<td style="text-align:center">哭</td>
<td style="text-align:right">大哥</td>
</tr>
<tr>
<td>关羽</td>
<td style="text-align:center">打</td>
<td style="text-align:right">二哥</td>
</tr>
<tr>
<td>张飞</td>
<td style="text-align:center">骂</td>
<td style="text-align:right">三弟</td>
</tr>
</tbody>
</table>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[node.js笔记]]></title>
        <id>https://merry-9001.github.io/post/nodejs</id>
        <link href="https://merry-9001.github.io/post/nodejs">
        </link>
        <updated>2019-11-08T12:33:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="nodejs">node.js</h1>
<ul>
<li>node.js没有dom和bom操作</li>
<li>Node.js 是什么
<ul>
<li>JavaScript 运行时</li>
<li>既不是语言，也不是框架，它是一个平台</li>
</ul>
</li>
<li>Node.js 中的 JavaScript
<ul>
<li>没有 BOM、DOM</li>
<li>EcmaScript 基本的 JavaScript 语言部分</li>
<li>在 Node 中为 JavaScript 提供了一些服务器级别的 API
<ul>
<li>文件操作的能力</li>
<li>http 服务的能力</li>
</ul>
</li>
</ul>
</li>
</ul>
<p></p>
<h2 id="使用-require-方法加载-fs-核心模块">使用 require 方法加载 fs 核心模块</h2>
<p>fs 是 file-system 的简写，就是文件系统的意思</p>
<pre><code>var fs = require('fs')
</code></pre>
<h2 id="读取文件">读取文件</h2>
<p>第一个参数就是要读取的文件路径<br>
第二个参数是一个回调函数</p>
<p>成功<br>
data 数据<br>
error null</p>
<p>失败<br>
data undefined没有数据<br>
error 错误对象</p>
<pre><code>fs.readFile('./data/a.txt', function (error, data) {

if (error) {
    console.log('读取文件失败了')
} else {
    console.log(data.toString())
}
})
</code></pre>
<h2 id="创建web服务器">创建web服务器</h2>
<p>1.加载 http 核心模块</p>
<pre><code>var http = require('http')
</code></pre>
<p>2.使用 http.createServer() 方法创建一个 Web 服务器<br>
返回一个 Server 实例</p>
<pre><code>var server = http.createServer()
</code></pre>
<p>3.服务器操作</p>
<pre><code>server.on('request', function () {
console.log('收到客户端的请求了')
})
</code></pre>
<p>4.绑定端口号，启动服务器</p>
<pre><code>server.listen(3000, function () {
console.log('服务器启动成功了，可以通过 http://127.0.0.1:3000/ 来进行访问')
})
</code></pre>
<h2 id="服务器接收参数">服务器接收参数</h2>
<p>Request 请求对象<br>
请求对象可以用来获取客户端的一些请求信息，例如请求路径<br>
Response 响应对象<br>
响应对象可以用来给客户端发送响应消息</p>
<h2 id="服务器操作">服务器操作</h2>
<p>在 http 协议中，Content-Type 就是用来告知对方我给你发送的数据内容是什么类型</p>
<pre><code>var url = req.url

if (url === '/plain') {
    // text/plain 就是普通文本
    res.setHeader('Content-Type', 'text/plain; charset=utf-8')
    res.end('hello 世界')
} else if (url === '/html') {
    // 如果你发送的是 html 格式的字符串，则也要告诉浏览器我给你发送是 text/html 格式的内容
    res.setHeader('Content-Type', 'text/html; charset=utf-8')
    res.end('&lt;p&gt;hello html &lt;a href=&quot;&quot;&gt;点我&lt;/a&gt;&lt;/p&gt;')
}
</code></pre>
<p>可以采用读取图片的方式，然后如果文件读取成功的话,end返回一个（data），data就是数据</p>
<pre><code>fs.readFile('./resource/ab2.jpg', function (err, data) {
  if (err) {
    res.setHeader('Content-Type', 'text/plain; charset=utf-8')
    res.end('文件读取失败，请稍后重试！')
  } else {
    // data 默认是二进制数据，可以通过 .toString 转为咱们能识别的字符串
    // res.end() 支持两种数据类型，一种是二进制，一种是字符串
    // 图片就不需要指定编码了，因为我们常说的编码一般指的是：字符编码
    res.setHeader('Content-Type', 'image/jpeg')
    res.end(data)
  }
})
</code></pre>
<h2 id="一个json数据类型接口">一个json数据类型接口</h2>
<pre><code>    var products = [{
        name: '苹果 X',
        price: 8888
    },
    {
        name: '菠萝 X',
        price: 5000
    },
    {
        name: '小辣椒 X',
        price: 1999
    }
    ]

    // 响应内容只能是二进制数据或者字符串
    //  数字
    //  对象
    //  数组
    //  布尔值
    res.end(JSON.stringify(products))
</code></pre>
<h2 id="nodejs核心模块">node.js核心模块</h2>
<p>用来获取机器信息的</p>
<pre><code>var os = require('os')
</code></pre>
<p>用来操作路径的</p>
<pre><code>var path = require('path')
</code></pre>
<p>获取当前机器的 CPU 信息</p>
<pre><code>console.log(os.cpus())
</code></pre>
<p>memory 内存</p>
<pre><code>console.log(os.totalmem())
</code></pre>
<p>获取一个路径中的扩展名部分</p>
<pre><code>extname extension name
console.log(path.extname('c:/a/b/c/d/hello.txt'))
</code></pre>
<h2 id="nodejs理论细节">node.js理论细节</h2>
<p>在 Node 中没有全局作用域的概念<br>
在 Node 中，只能通过 require 方法来加载执行多个 JavaScript 脚本文件<br>
require 加载只能是执行其中的代码，文件与文件之间由于是模块作用域，所以不会有污染的问题</p>
<ul>
<li>模块完全是封闭的</li>
<li>外部无法访问内部</li>
<li>内部也无法访问外部<br>
模块作用域固然带来了一些好处，可以加载执行多个文件，可以完全避免变量命名冲突污染的问题</li>
</ul>
<ul>
<li>但是某些情况下，模块与模块是需要进行通信的</li>
<li>在每个模块中，都提供了一个对象：<code>exports</code></li>
<li>该对象默认是一个空对象</li>
<li>你要做的就是把需要被外部访问使用的成员手动的挂载到 <code>exports</code> 接口对象中</li>
<li>然后谁来 <code>require</code> 这个模块，谁就可以得到模块内部的 <code>exports</code> 接口对象</li>
</ul>
<h2 id="核心模块">核心模块</h2>
<p>核心模块是由 Node 提供的一个个的具名的模块，它们都有自己特殊的名称标识，例如</p>
<ul>
<li>fs 文件操作模块</li>
<li>http 网络服务构建模块</li>
<li>os 操作系统信息模块</li>
<li>path 路径处理模块</li>
</ul>
<h2 id="http">http</h2>
<ul>
<li>Content-Type
<ul>
<li>服务器最好把每次响应的数据是什么内容类型都告诉客户端，而且要正确的告诉</li>
<li>不同的资源对应的 Content-Type 是不一样，具体参照：http://tool.oschina.net/commons</li>
<li>对于文本类型的数据，最好都加上编码，目的是为了防止中文解析乱码问题</li>
</ul>
</li>
<li>通过网络发送文件
<ul>
<li>发送的并不是文件，本质上来讲发送是文件的内容</li>
<li>当浏览器收到服务器响应内容之后，就会根据你的 Content-Type 进行对应的解析处理</li>
</ul>
</li>
</ul>
<h2 id="关于js代码是否使用分号">关于js代码是否使用分号</h2>
<p>建议如果一行代码是以 (、[、` 开头的，则最好都在其前面补上一个分号。</p>
<h2 id="art-template模板">art-template模板</h2>
<ol>
<li>安装 npm install art-template</li>
<li>在需要使用的文件模块中加载 art-template<br>
只需要使用 require 方法加载就可以了：require('art-template')<br>
参数中的 art-template 就是你下载的包的名字<br>
也就是说你 isntall 的名字是什么，则你 require 中的就是什么</li>
<li>查文档，使用模板引擎的 API</li>
</ol>
<h2 id="服务端渲染和客户端渲染的区别">服务端渲染和客户端渲染的区别</h2>
<ul>
<li>客户端渲染不利于 SEO 搜索引擎优化</li>
<li>服务端渲染是可以被爬虫抓取到的，客户端异步渲染是很难被爬虫抓取到的</li>
<li>所以你会发现真正的网站既不是纯异步也不是纯服务端渲染出来的</li>
<li>而是两者结合来做的</li>
<li>例如京东的商品列表就采用的是服务端渲染，目的了为了 SEO 搜索引擎优化</li>
<li>而它的商品评论列表为了用户体验，而且也不需要 SEO 优化，所以采用是客户端渲染</li>
</ul>
<h2 id="moduleexports和exports">module.exports和exports</h2>
<p>谁 require 我，谁就得到 module.exports<br>
默认在代码的最后有一句：<br>
一定要记住，最后 return 的是 module.exports，而不是 exports<br>
可以只使用module.exports</p>
<h2 id="moduleexports-和-exports-的区别">module.exports 和 exports 的区别</h2>
<ul>
<li>每个模块中都有一个 module 对象</li>
<li>module 对象中有一个 exports 对象</li>
<li>我们可以把需要导出的成员都挂载到 module.exports 接口对象中</li>
<li>也就是：<code>moudle.exports.xxx = xxx</code> 的方式</li>
<li>但是每次都 <code>moudle.exports.xxx = xxx</code> 很麻烦，点儿的太多了</li>
<li>所以 Node 为了你方便，同时在每一个模块中都提供了一个成员叫：<code>exports</code></li>
<li><code>exports === module.exports</code> 结果为  <code>true</code>s</li>
<li>所以对于：<code>moudle.exports.xxx = xxx</code> 的方式 完全可以：<code>expots.xxx = xxx</code></li>
<li>当一个模块需要导出单个成员的时候，这个时候必须使用：<code>module.exports = xxx</code> 的方式</li>
<li>不要使用 <code>exports = xxx</code> 不管用</li>
<li>因为每个模块最终向外 <code>return</code> 的是 <code>module.exports</code></li>
<li>而 <code>exports</code> 只是 <code>module.exports</code> 的一个引用</li>
<li>所以即便你为 <code>exports = xx</code> 重新赋值，也不会影响 <code>module.exports</code></li>
<li>但是有一种赋值方式比较特殊：<code>exports = module.exports</code> 这个用来重新建立引用关系的</li>
</ul>
<hr>
<ul>
<li>exports 只是 module.exports 的一个引用而已，目的只是为了简化写法</li>
<li>每个模块最终 return 的是 module.exports</li>
</ul>
<h2 id="加载模块">加载模块</h2>
<ul>
<li>
<p>路径形式的模块：<br>
./ 当前目录，不可省略<br>
../ 上一级目录，不可省略</p>
</li>
<li>
<p>核心模块：<br>
核心模块的本质也是文件<br>
核心模块文件已经被编译到了二进制文件中了，我们只需要按照名字来加载就可以了<br>
require('fs')<br>
require('http')</p>
</li>
<li>
<p>第三方模块<br>
凡是第三方模块都必须通过 npm 来下载<br>
使用的时候就可以通过 require('包名') 的方式来进行加载才可以使用<br>
不可能有任何一个第三方包和核心模块的名字是一样的<br>
既不是核心模块、也不是路径形式的模块</p>
</li>
</ul>
<h1 id="express">Express</h1>
<h2 id="介绍">介绍</h2>
<ul>
<li>第三方 Web 开发框架</li>
<li>高度封装了 http 模块</li>
<li>更加专注于业务，而非底层细节</li>
<li>知其所以然</li>
</ul>
<ul>
<li>增删改查
<ul>
<li>使用文件来保存数据（锻炼异步编码）</li>
</ul>
</li>
<li>MongoDB
<ul>
<li>（所有方法都封装好了）</li>
</ul>
</li>
</ul>
<h2 id="使用">使用</h2>
<ol>
<li>
<p>安装npm</p>
</li>
<li>
<p>引包</p>
<p>var express = require('express')</p>
</li>
<li>
<p>创建你服务器应用程序</p>
<p>var app = express()</p>
</li>
</ol>
<h3 id="开放资源">开放资源</h3>
<p>只要这样做了，你就可以直接通过 /public/xx 的方式访问 public 目录中的所有资源了</p>
<pre><code>app.use('/public/', express.static('./public/'))
</code></pre>
<p>用户访问public地址的时候，给他显示public接口</p>
<h3 id="get请求">get请求</h3>
<pre><code>app.get('/about', function (req, res) {
// 在 Express 中可以直接 req.query 来获取查询字符串参数
console.log(req.query)
res.send('你好，我是 Express!')
})
</code></pre>
<h3 id="开启接口">开启接口</h3>
<p>相当于 server.listen</p>
<pre><code>app.listen(3000, function () {
console.log('app is running at port 3000.')
})
</code></pre>
<h2 id="使用模板">使用模板</h2>
<ol>
<li>
<p>安装express-art-template和art-template</p>
</li>
<li>
<p>配置 html表示使用的是html结尾的模板才能使用</p>
<p>app.engine('html', require('express-art-template'))</p>
<figure data-type="image" tabindex="1"><img src="https://merry-9001.github.io/post-images/1576909355217.png" alt=""></figure>
</li>
</ol>
<h2 id="获取表单内容">获取表单内容</h2>
<ol>
<li>
<p>安装body-parser</p>
</li>
<li>
<p>配置</p>
<p>var bodyParser = require('body-parser')<br>
// 配置模板引擎和 body-parser 一定要在 app.use(router) 挂载路由之前<br>
// parse application/x-www-form-urlencoded<br>
app.use(bodyParser.urlencoded({ extended: false }))<br>
// parse application/json<br>
app.use(bodyParser.json())</p>
</li>
</ol>
<h1 id="回调函数">回调函数</h1>
<h2 id="如果需要获取一个函数中异步操作的结果则必须通过回调函数来获取">如果需要获取一个函数中异步操作的结果，则必须通过回调函数来获取</h2>
<pre><code>function fn(callback) {
    setTimeout(function() {
        var data = 'hello'
        callback(data)
    }, 1000)
}
fn(function(data) {
    console.log(data)
})
</code></pre>
<p>如果想取到data的数据进行处理，就只能进行回调函数，因为定时器是异步的，函数不会等待执行完毕就直接结束了，只有在调用的时候传递一个函数给里面的定时器才行</p>
<h2 id="异步">异步</h2>
<h2 id="一般情况下把函数作为参数的目的就是为了获取函数内部的异步操作结果javascript-单线程-事件循环">一般情况下，把函数作为参数的目的就是为了获取函数内部的异步操作结果<br>
JavaScript 单线程、事件循环</h2>
<p>异步操作的函数</p>
<ul>
<li>setTimeout</li>
<li>readFile</li>
<li>writeFile</li>
<li>ajax</li>
</ul>
<h2 id="promise">Promise</h2>
<p><img src="https://merry-9001.github.io/post-images/1576911008130.png" alt=""><br>
<img src="https://merry-9001.github.io/post-images/1576911012957.png" alt=""></p>
<h3 id="特点">特点</h3>
<h4 id="对象的状态不受外界影响-3种状态">对象的状态不受外界影响 （3种状态）</h4>
<ul>
<li>Pending状态（进行中）</li>
<li>Fulfilled状态（已成功）</li>
<li>Rejected状态（已失败）</li>
</ul>
<h4 id="一旦状态改变就不会再变-两种状态改变成功或失败">一旦状态改变就不会再变 （两种状态改变：成功或失败）</h4>
<ul>
<li>Pending -&gt; Fulfilled</li>
<li>Pending -&gt; Rejected</li>
</ul>
<h3 id="例子">例子</h3>
<pre><code>var p1 = new Promise(function (resolve, reject) {
fs.readFile('./data/a.txt', 'utf8', function (err, data) {
    if (err) {
    reject(err)
    } else {
    resolve(data)
    }
})
})

var p2 = new Promise(function (resolve, reject) {
fs.readFile('./data/b.txt', 'utf8', function (err, data) {
    if (err) {
    reject(err)
    } else {
    resolve(data)
    }
})
})

var p3 = new Promise(function (resolve, reject) {
fs.readFile('./data/c.txt', 'utf8', function (err, data) {
    if (err) {
    reject(err)
    } else {
    resolve(data)
    }
})
})

p1
.then(function (data) {
    console.log(data)
    return p2
}, function (err) {
    console.log('读取文件失败了', err)
})
.then(function (data) {
    console.log(data)
    return p3
})
.then(function (data) {
    console.log(data)
    console.log('end')
})
</code></pre>
<h2 id="当-p1-读取成功的时候当前函数中-return-的结果就可以在后面的-then-中-function-接收到当你-return-123-后面就接收到-123">当 p1 读取成功的时候<br>
当前函数中 return 的结果就可以在后面的 then 中 function 接收到<br>
当你 return 123 后面就接收到 123</h2>
<p>我们可以 return 一个 Promise 对象<br>
当 return 一个 Promise 对象的时候，后续的 then 中的 方法的第一个参数会作为 p2 的 resolve</p>
<h3 id="我觉得利用promise之后就不用层层嵌套了而且执行的顺序依旧是按照代码的顺序来执行不会乱">我觉得利用promise之后就不用层层嵌套了，而且执行的顺序依旧是按照代码的顺序来执行，不会乱</h3>
<pre><code>function pReadFile(filePath) {
    return new Promise(function (resolve, reject) {
        fs.readFile(filePath, 'utf8', function (err, data) {
        if (err) {
            reject(err)
        } else {
            resolve(data)
        }
        })
    })
}

pReadFile('./data/a.txt')
.then(function (data) {
    console.log(data)
    return pReadFile('./data/b.txt')
})
.then(function (data) {
    console.log(data)
    return pReadFile('./data/c.txt')
})
.then(function (data) {
    console.log(data)
})
</code></pre>
<h1 id="__dirname-和-__filename">__dirname 和 __filename</h1>
<ul>
<li>动态的获取当前文件或者文件所处目录的绝对路径</li>
<li>用来解决文件操作路劲的相对路径问题</li>
<li>因为在文件操作中，相对路径相对于执行 <code>node</code> 命令所处的目录</li>
<li>所以为了尽量避免这个问题，都建议文件操作的相对路劲都转为：<strong>动态的绝对路径</strong></li>
<li>方式：<code>path.join(__dirname, '文件名')</code></li>
</ul>
<p>用了这个他就会帮助你获取当前的电脑的绝对路径，避免了相对路径会遇到的问题也不至于会写死<br>
__dirname是用来获取文件的目录<br>
__filename可以用来获取当前的文件名<br>
就是专门用来动态的获取当前文件以及文件所属目录的绝对路径</p>
<h1 id="mysql">Mysql</h1>
<h2 id="例子-2">例子</h2>
<p>先导包(npm)<br>
再使用<br>
var mysql = require('mysql');</p>
<pre><code>// 1. 创建连接
var connection = mysql.createConnection({
host: 'localhost',
user: 'root',
password: 'root',
database: 'users' // 对不起，我一不小心把数据库名字和表名起成一样的，你知道就行
});

// 2. 连接数据库 打开冰箱门
connection.connect();

// 3. 执行数据操作 把大象放到冰箱
connection.query('SELECT * FROM `users`', function (error, results, fields) {
if (error) throw error;
console.log('The solution is: ', results);
});

// connection.query('INSERT INTO users VALUES(NULL, &quot;admin&quot;, &quot;123456&quot;)', function (error, results, fields) {
//   if (error) throw error;
//   console.log('The solution is: ', results);
// });

// 4. 关闭连接 关闭冰箱门
connection.end();
</code></pre>
<h1 id="mongodb-数据库">MongoDB 数据库</h1>
<h2 id="mongodb-的数据存储结构">MongoDB 的数据存储结构</h2>
<ul>
<li>数据库</li>
<li>集合（表）</li>
<li>文档（表记录）</li>
</ul>
<h2 id="mongodb-官方有一个-mongodb-的包可以用来操作-mongodb-数据库">MongoDB 官方有一个 mongodb 的包可以用来操作 MongoDB 数据库</h2>
<ul>
<li>这个确实和强大，但是比较原始，麻烦，所以咱们不使用它</li>
</ul>
<h2 id="mongoose">mongoose</h2>
<ul>
<li>真正在公司进行开发，使用的是 mongoose 这个第三方包</li>
<li>它是基于 MongoDB 官方的 mongodb 包进一步做了封装</li>
<li>可以提高开发效率</li>
<li>让你操作 MongoDB 数据库更方便</li>
</ul>
<h2 id="安装mongodb数据库">安装mongodb数据库</h2>
<ol>
<li>
<p>解压文件</p>
</li>
<li>
<p>配置环境变量<br>
<img src="https://merry-9001.github.io/post-images/1576913007511.png" alt=""></p>
<p>C:\Users\six\Downloads\mongodb\mongodb-win32-x86_64-2008plus-ssl-4.0.3\bin</p>
</li>
<li>
<p>cmd里面输入 mongo -version测试</p>
</li>
</ol>
<h2 id="启动mongodb数据库">启动mongodb数据库</h2>
<pre><code>cmd中输入mongod(cmd1)
mongo show dbs(cmd2)
</code></pre>
<h2 id="mongoose包">mongoose包</h2>
<ol>
<li>
<p>安装</p>
</li>
<li>
<p>引入</p>
<p>var mongoose = require('mongoose')</p>
</li>
</ol>
<h3 id="例子-3">例子</h3>
<pre><code>var mongoose = require('mongoose')

// 连接数据库
mongoose.connect('mongodb://localhost/test', { useMongoClient: true })

var Schema = mongoose.Schema

var userSchema = new Schema({
email: {
    type: String,
    required: true
},
nickname: {
    type: String,
    required: true
}
})

module.exports = mongoose.model('User', userSchema)
</code></pre>
<p>只需要执行代码就会创建使用，如果存在的话就可以直接用.<br>
localhost/表名</p>
<h2 id="操作数据库">操作数据库</h2>
<h3 id="mongodb数据库自己会创建一个主键唯一标识">mongodb数据库自己会创建一个主键（唯一标识）</h3>
<h3 id="select-了解一下">select * 了解一下</h3>
<pre><code>Student.find(function(err, students) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.render('index.html', {
        fruits: [
            '苹果',
            '香蕉',
            '橘子'
        ],
        students: students
    })
})
</code></pre>
<h3 id="条件查询">条件查询</h3>
<pre><code>var body = req.body

User.findOne({
    email: body.email,
    password: md5(md5(body.password))
}, function (err, user) {
    if (err) {
    return res.status(500).json({
        err_code: 500,
        message: err.message
    })
}

{
    $or: [{
        email: body.email
    },
    {
        nickname: body.nickname
    }
    ]
}
</code></pre>
<p>or条件与and条件</p>
<h3 id="插入">插入</h3>
<pre><code>new Student(req.body).save(function(err) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.redirect('/students')
})
</code></pre>
<h3 id="查询一条记录">查询一条记录</h3>
<pre><code>Student.findById(req.query.id.replace(/&quot;/g, ''), function(err, student) {
    if (err) {
        console.log(err)
        return res.status(500).send('Server error.')
    }
    res.render('edit.html', {
        student: student
    })
})
</code></pre>
<h3 id="更新数据">更新数据</h3>
<pre><code>var id = req.body.id.replace(/&quot;/g, '')
Student.findByIdAndUpdate(id, req.body, function(err) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.redirect('/students')
})
</code></pre>
<h3 id="删除数据">删除数据</h3>
<pre><code>var id = req.query.id.replace(/&quot;/g, '')
Student.findByIdAndRemove(id, function(err) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.redirect('/students')
})        
</code></pre>
<p>查找一条数据，第一个参数传条件，第二参数是一个函数，用来执行回调方法</p>
<h1 id="session">session</h1>
<h3 id="装包引用包">装包，引用包</h3>
<pre><code>var session = require('express-session')
</code></pre>
<h3 id="配置">配置</h3>
<pre><code>app.use(session({
    // 配置加密字符串，它会在原有加密基础之上和这个字符串拼起来去加密
    // 目的是为了增加安全性，防止客户端恶意伪造
    secret: 'itcast',
    resave: false,
    saveUninitialized: false // 无论你是否使用 Session ，我都默认直接给你分配一把钥匙
}))
</code></pre>
<h3 id="使用-2">使用</h3>
<pre><code>当把这个插件配置好之后，我们就可以通过 req.session 来发访问和设置 Session 成员了
添加 Session 数据：req.session.foo = 'bar'
访问 Session 数据：req.session.foo
</code></pre>
<h3 id="例子-4">例子</h3>
<pre><code>res.render('index.html', {
    user: req.session.user
})

req.session.user = user

// 清除登陆状态
req.session.user = null
</code></pre>
<h1 id="packagejson">package.json</h1>
<h2 id="介绍-2">介绍</h2>
<ul>
<li>就是产品的说明书</li>
<li><code>dependencies</code> 属性，用来保存项目的第三方包依赖项信息</li>
<li>所以建议每个项目都要有且只有一个 package.json (存放在项目的根目录)</li>
<li>我们可以通过 <code>npm init [--yes]</code> 来生成 package.json 文件</li>
<li>同样的，为了保存依赖项信息，我们每次安装第三方包的时候都要加上：<code>--save</code> 选项。</li>
</ul>
<p>在文件夹cmd中输入npm init，获取package.json文件<br>
然后你使用npm装包就可以看见，package.json-lock可以帮助锁定版本，也可以提高速速</p>
<h1 id="模板继承">模板继承</h1>
<h2 id="art-template例子">art-template例子</h2>
<p>layout.html<br>
<img src="https://merry-9001.github.io/post-images/1576921668859.png" alt=""></p>
<p>index.html<br>
<img src="https://merry-9001.github.io/post-images/1576921676633.png" alt=""></p>
<p>这样使用以后，加载index.html的时候就可以自动先去找layout，非常好用，<strong>方便复用</strong></p>
<h1 id="中间件">中间件</h1>
<h3 id="中间件处理请求的本质就是个函数">中间件：处理请求的，本质就是个函数</h3>
<p>当请求进来，会从第一个中间件开始进行匹配</p>
<ul>
<li>如果匹配，则进来
<ul>
<li>如果请求进入中间件之后，没有调用 next 则代码会停在当前中间件</li>
<li>如果调用了 next 则继续向后找到第一个匹配的中间件</li>
</ul>
</li>
<li>如果不匹配，则继续判断匹配下一个中间件</li>
</ul>
<p><strong>不关心请求路径和请求方法的中间件</strong></p>
<p><strong>也就是说任何请求都会进入这个中间件</strong></p>
<p>中间件本身是一个方法，该方法接收三个参数：<br>
Request 请求对象<br>
Response 响应对象<br>
next     下一个中间件</p>
<p>通配</p>
<pre><code>app.use(function (req, res, next) {
console.log(1)
next()
})
</code></pre>
<p>以 /a 开头的路径中间件</p>
<pre><code>app.use('/a', function (req, res, next) {
console.log('a')
next()
})
</code></pre>
<p>精准匹配<br>
app.get('/abc', function(req, res, next) {<br>
console.log('abc')<br>
next()<br>
})</p>
<h3><font color="red">如果不写next()就不会继续执行下个中间件
其实之前用的很多插件使用use的配置都是采用了中间件，然后next()到下一个
所以才能使用那些方法
</font></h3>
<p>配置错误处理中间件</p>
<pre><code>next(err)
app.use(function (err, req, res, next) {
    es.status(500).send(err.message)
})
</code></pre>
<p>如果使用中间的next()里面传递了参数，那么就会直接进入错误处理中间件，这个中间件必须拥有四个参数</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[毕业设计文献]]></title>
        <id>https://merry-9001.github.io/post/bi-ye-she-ji-wen-xian</id>
        <link href="https://merry-9001.github.io/post/bi-ye-she-ji-wen-xian">
        </link>
        <updated>2019-11-08T12:32:04.000Z</updated>
        <content type="html"><![CDATA[<p>基于PHP技术的校园综合服务网站的建设<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=JYJU201844038&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEWFBUZXJFRFIxSmYzbi9ydz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDM2OTkxRnJDVVJMT2VaK1JvRnl2Z1ZyektMelRCZTdHNEg5bklxNDlHYklSOGVYMUx1eFlTN0RoMVQzcVRyV00=</p>
<p>毕业季私人定制服务营销策略浅析<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=ZQGZ201806023&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEWE5kK3FTK09mL2JsMXNibz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjM0OThNMUZyQ1VSTE9lWitSb0Z5dmhVNy9QUHp6TWRMRzRIOW5NcVk5SFo0UjhlWDFMdXhZUzdEaDFUM3FUclc=</p>
<p>基于Liferay平台的私人订制功能及应用<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CMFD&amp;dbname=CMFD201702&amp;filename=1017193354.nh&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEV3RzN3ZsRlFFQjQraUdDRT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjIwNDIxTHV4WVM3RGgxVDNxVHJXTTFGckNVUkxPZVorUm9GeXZoVWIvQlZGMjZHYkt4SGRMSnE1RWJQSVI4ZVg=</p>
<p>[1]杨嘉鑫.校园最后一公里配送服务研究——以A大学城为例[J].价值工程,2019,38(24):232-233.<br>
校园最后一公里配送服务研究——以A大学城为例<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2019&amp;filename=JZGC201924097&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEV3RzN3ZsRlFFQjQraUdDRT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MTk4MjFYMUx1eFlTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnl2aFY3L0xMemZNYmJHNEg5ak9xNDlNWTRSOGU=</p>
<p>基于java语言的校园服务平台设计<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2017&amp;filename=RJDJ201710035&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEV3N1eXNhaUdRbkovOFFQVT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDkwMzJUM3FUcldNMUZyQ1VSTE9lWitSb0Z5cmxVN3JJTnlmUFpMRzRIOWJOcjQ5R1lZUjhlWDFMdXhZUzdEaDE=</p>
<p>基于Vue.js框架的掌上零售系统的设计与实现<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CMFD&amp;dbname=CMFD201901&amp;filename=1019601282.nh&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFRk1DMU5tbHY3NEVEemhDYz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjY2MjFyQ1VSTE9lWitSb0Z5cmxWNy9KVkYyNkY3VzRIOVBFclpFYlBJUjhlWDFMdXhZUzdEaDFUM3FUcldNMUY=</p>
<p>一种校园生活一站式服务平台的设计与实现<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2019&amp;filename=XXDL201910026&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFRk1DMU5tbHY3NEVEemhDYz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjI1MDk5ak5yNDlIWW9SOGVYMUx1eFlTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnlybVU3N05QVFhQWXJHNEg=</p>
<p>智慧校园移动系统数据库设计<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=AYSF201802015&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFR2paYklwblFIQjhUcEh2UT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDA5Njk5bk1yWTlFWVlSOGVYMUx1eFlTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnlybVVMckpKRFRZYUxHNEg=</p>
<p>校园网二级网站建设数据库设计<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2016&amp;filename=DZRU201609156&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFR2psdVFVUVNwM3lVUk5lND0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDkzOTBoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnlybVViM0JJVGZaZTdHNEg5Zk1wbzVBWW9SOGVYMUx1eFlTN0Q=</p>
<p>基于HTML5响应式页面的研究与应用<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=NMKJ201818031&amp;uid=WEEvREdxOWJmbC9oM1NjYkZCbDdrNXcxeGc0Mjd1bU1iTDMwSCtLeEhGVXM=$R1yZ0H6jyaa0en3RxVUd8df-oHi7XMMDo7mtKT6mSmEvTuk11l2gFA!!&amp;v=MDczNDdTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnk3aFY3ek9LeURBWkxHNEg5bk5wNDlHWllSOGVYMUx1eFk=</p>
<p>基于Vue.js的后台单页应用管理系统的研究与实现<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2017&amp;filename=XDJS201730011&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVVVVFQb0tLMWpjNnpNTUMrTT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MTMxMzlLUFNuQmZiRzRIOWJQcjQ5RVpZUjhlWDFMdXhZUzdEaDFUM3FUcldNMUZyQ1VSTE9lWitSb0Z5N2hXcjc=</p>
<p>纸媒突围:提供订制服务<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2015&amp;filename=PXWS201412009&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVVVVFQb0tLMWpjNnpNTUMrTT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDg3OTkxRnJDVVJMT2VaK1JvRnlIa1ZML09OVFhjZmJHNEg5WE5yWTlGYllSOGVYMUx1eFlTN0RoMVQzcVRyV00=</p>
<p>MySQL数据库备份及恢复方法解析<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDTEMP&amp;filename=WLAQ201910038&amp;uid=WEEvREcwSlJHSldRa1Fhb09jT0lPd1RLUG54SzhxYUFTWlpDZ3Yrcm00cz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MTYyNTJGeURoVkwvSU1pSEtmN0c0SDlqTnI0OUdiSVI4ZVgxTHV4WVM3RGgxVDNxVHJXTTFGckNVUkxPZVorUm8=</p>
<p>Using collaborative filtering to support college students’ hu2014</p>
<p>use of online forum for English learning wang2012</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[npm]]></title>
        <id>https://merry-9001.github.io/post/npm</id>
        <link href="https://merry-9001.github.io/post/npm">
        </link>
        <updated>2019-10-25T02:08:52.000Z</updated>
        <content type="html"><![CDATA[<p>下载node获得npm</p>
<p>可以安装nrm<br>
npm i nrm -g</p>
<p>样可以下包使用cnpm<br>
npm i cnpm -g这</p>
<p>可以查看<br>
nrm ls</p>
<p>可以使用cnpm管理<br>
nrm use npm</p>
<p>使用淘宝镜像的命令：</p>
<p>npm install -g cnpm --registry=https://registry.npm.taobao.org</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[git操作]]></title>
        <id>https://merry-9001.github.io/post/git-cao-zuo</id>
        <link href="https://merry-9001.github.io/post/git-cao-zuo">
        </link>
        <updated>2019-10-25T02:02:53.000Z</updated>
        <content type="html"><![CDATA[<p>git 需要配置邮箱 账号</p>
<figure data-type="image" tabindex="1"><img src="https://merry-9001.github.io/post-images/1571969004460.jpg" alt=""></figure>
<p>然后设置公钥（在github里面）</p>
<p>查看仓库<br>
git remote</p>
<p>创建仓库<br>
git remote add origin 地址</p>
<p>创建分支<br>
git checkout -b &quot;dev&quot;</p>
<p>查看状态<br>
git status</p>
<p>添加<br>
git add .</p>
<p>提交（注释）<br>
git commit -m &quot;内容&quot;</p>
<p>切换分支<br>
git checkout master</p>
<p>合并分支<br>
git merge dev --no-ff</p>
<p>退出注释<br>
: q</p>
<p>查看日志<br>
git log</p>
<p>上传至github<br>
git push origin master</p>
<p>查看分支<br>
git branch</p>
<p>删除<br>
git branch -d dev</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[vue]]></title>
        <id>https://merry-9001.github.io/post/vue</id>
        <link href="https://merry-9001.github.io/post/vue">
        </link>
        <updated>2019-10-25T01:39:05.000Z</updated>
        <content type="html"><![CDATA[<h1>router-link高亮</h1>
<figure data-type="image" tabindex="1"><img src="https://merry-9001.github.io/post-images/1571968592828.jpg" alt=""></figure>
<h1>vue修改json传过来的data</h1>
<figure data-type="image" tabindex="2"><img src="https://merry-9001.github.io/post-images/1571968640410.jpg" alt=""></figure>
<h1>vue-rend渲染简写</h1>
<p><img src="https://merry-9001.github.io/post-images/1571968666697.jpg" alt=""><br>
<img src="https://merry-9001.github.io/post-images/1571968670196.jpg" alt=""></p>
<h1>vue-cli创建</h1>
<p>npm install -g @vue/cli</p>
<p>vue create &quot;项目名&quot;</p>
<p>最后一个自定义配置</p>
<p>babel router vuex css 选中四个 （开发相关） 回车</p>
<p>y</p>
<p>选第一种sass</p>
<p>选中package.json</p>
<p>预设配置 不需要拍</p>
<p>npm i -s axios</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ES6]]></title>
        <id>https://merry-9001.github.io/post/es6</id>
        <link href="https://merry-9001.github.io/post/es6">
        </link>
        <updated>2019-10-24T11:04:57.000Z</updated>
        <content type="html"><![CDATA[<h1 id="es6怎么来的">ES6怎么来的</h1>
<ul>
<li>ECMAScript 和 JavaScript
<ul>
<li>ECMA 是标准，JS 是实现</li>
<li>ECMAScript 简称 ECMA 或 ES</li>
</ul>
</li>
</ul>
<h1 id="let和const">let和const</h1>
<p>解决问题</p>
<p>​	var不能用于定义常量</p>
<p>​	var可以重复声明变量</p>
<p>​	var存在变量提升</p>
<p>​	var不支持块级作用域</p>
<p>不可以重复声明变量</p>
<pre><code>    let site = 'itLike';

​    let site = 'itLike'; 

​    console.log(site);
</code></pre>
<p>不存在变量提升</p>
<pre><code>    console.log(site);

​    let site = 'itLike';
</code></pre>
<p>const用来定义常量</p>
<pre><code>    const E = 2.718;
</code></pre>
<p>块级作用域用一组大括号定义一个块，使用 let 定义的变量在大括号的外部是访问不到的，此外，let声明的变量不会污染全局作用域。（let拥有块级作用域）</p>
<pre><code>{let site = 'itLike';}
</code></pre>
<h1 id="解构赋值">解构赋值</h1>
<pre><code>let [a, b, c] = [1, 2, 3]
console.log(a, b, c)

let {x, y, z} = {x: 1, y: 2, z: 3}
console.log(x, y, z)

let [json, arr, num, str] = [{ a: 1, b: 2 }, [1, 2, 3], 8, 'str']
console.log(json, arr, num, str)


let [name1, name2, name3] = nameArr;
let {name: lkName, age: lkAge, sex: lkSex}= {name: '小煤球', age: 1, sex: '公'};
</code></pre>
<ul>
<li>解构赋值
<ul>
<li>左右两个边结构必须一样</li>
<li>右边必须是个东西</li>
<li>声明和赋值赋值不能分开，必须在一句话里</li>
</ul>
</li>
</ul>
<h1 id="函数操作">函数操作</h1>
<p>设置默认参数</p>
<pre><code>    function logPerson(name, sex = '男', age = 20) {

​        console.log(name);

​        console.log(sex);

​        console.log(age);

​    }

​    undefined 男 20

​    logPerson();
</code></pre>
<h1 id="函数-参数">函数-参数</h1>
<pre><code>    let logName = function (arg1, ...arg2) {

​        console.log(arg1, arg2);

​    };

​    // 宋小宝 , [&quot;赵薇&quot;, &quot;王菲&quot;, &quot;那英&quot;]

​    logName('宋小宝', '赵薇', '王菲', '那英');
</code></pre>
<ul>
<li>
<p>参数扩展／展开</p>
<pre><code>...args
</code></pre>
<ul>
<li>收集剩余的参数，必须当到最后一个参数位置</li>
<li>展开数组，简写，效果和直接把数组的内容写在这儿一样</li>
</ul>
</li>
<li>
<p>默认参数</p>
</li>
</ul>
<pre><code>function show(a, b, ...args) {
    console.log(a)
    console.log(b)
    console.log(args)
}
console.log(show(1, 2, 3, 4, 5))

let arr1 = [1, 2, 3]
let arr2 = [4, 5, 6]
let arr3 = [...arr1, ...arr2]
console.log(arr3)

function show2(a, b=5, c=8) {
    console.log(a, b, c)
}
show2(88, 12)
</code></pre>
<h1>super</h1>
<p>通过super可以调用prototype上的属性或方法。</p>
<h1 id="类操作">类操作</h1>
<p>在之前定义类是通过构造函数来操作的，es6新增了class关键字，此时，我们也可以像其它语言一样来创建各种类了。</p>
<pre><code>    class Person{

​        constructor(name, age){

​            this.name = name;

​            this.age = age;

​        }

​        print(){

​            console.log('我叫' + this.name + ',今年' + this.age + '岁');

​        }

​    }
</code></pre>
<h1 id="集合操作">集合操作</h1>
<p>1）set</p>
<p>一个Set是一堆东西的集合，Set有点像数组，不过跟数组不一样的是，Set里面不能有重复的内容；</p>
<ol start="2">
<li>map 两个值</li>
</ol>
<p>Map结构提供了“值—值”的对应，是一种更完善的Hash结构实现。</p>
<p>如果你需要“键值对”的数据结构，Map比Object更合适。</p>
<p>它类似于对象，也是键值对的集合，但是“键”的范围不限于字符串，各种类型的值（包括对象）都可以当作键。</p>
<p>实例属性和方法：size、set、get、has、delete、clear</p>
<h1 id="重载">重载</h1>
<p>什么时重载？</p>
<p>ES6带来了Proxy和Reflect，配合使用可以实现重载。</p>
<p>Proxy用于修改某些操作的默认行为，相当于对原始想进行的操作进行“包装”；</p>
<p>Reflect对象的方法与Proxy对象的方法一一对应，这使得Proxy对象可以方便的调用对应的Reflect方法完成默认行为。</p>
<p>简单说，就是函数或者方法有相同的名称，但是参数列表不相同的情形，这样的同名不同参数的函数或者方法之间，互相称之为重载函数或者方法。</p>
<h1 id="数组">数组</h1>
<ul>
<li>新增4个方法</li>
<li>map 映射 一个对一个</li>
</ul>
<pre><code>let arr = [12, 5, 8]
let result = arr.map(function (item) {
    return item*2
})
let result2 = arr.map(item=&gt;item*2) // 简写
console.log(result)
console.log(result2)

let score = [18, 86, 88, 24]
let result3 = score.map(item =&gt; item &gt;= 60 ? '及格' : '不及格')
console.log(result3)

// 结果
[ 24, 10, 16 ]
[ 24, 10, 16 ]
[ '不及格', '及格', '及格', '不及格' ]
</code></pre>
<ul>
<li>reduce 汇总 一堆出来一个
<ul>
<li>用于比如，算个总数，算个平均</li>
</ul>
</li>
</ul>
<pre><code>var arr = [1, 3, 5, 7]
var result = arr.reduce(function (tmp, item, index) {
    //tmp 上次结果，item当前数，index次数1开始
    console.log(tmp, item, index)
    return tmp + item
})
console.log(result)

var arr = [1, 3, 5, 7]
var result = arr.reduce(function (tmp, item, index) {
    if (index != arr.length - 1) { // 不是最后一次
        return tmp + item
    } else {
        return (tmp + item)/arr.length
    }
})
console.log(result)  // 平均值
</code></pre>
<ul>
<li>filter 过滤器 保留为true的</li>
</ul>
<pre><code>var arr = [12, 4, 8, 9]
var result = arr.filter(item =&gt; (item % 3 === 0) ? true : false)
console.log(result)
var result = arr.filter(item =&gt; item % 3 === 0)
console.log(result)

var arr = [
    { title: '苹果', price: 10 },
    { title: '西瓜', price: 20 },
]
var result = arr.filter(json =&gt; json.price &gt;= 20)
console.log(result)
</code></pre>
<ul>
<li>forEach 循环迭代</li>
</ul>
<pre><code>var arr = [12, 4, 8, 9]
var result = arr.forEach(item =&gt; console.log(item))
var result = arr.forEach((item, index)=&gt;console.log(item, index))
</code></pre>
<h1 id="字符串">字符串</h1>
<ul>
<li>多了两个新方法
<ul>
<li><code>startsWith</code></li>
<li><code>endsWith</code></li>
</ul>
</li>
</ul>
<pre><code>var url = 'http://qq.com'
console.log(url.startsWith('http'))
console.log(url.endsWith('com'))
// 都是 true
</code></pre>
<ul>
<li>字符串模版
<ul>
<li>使用反引号，<code>${变量}</code></li>
<li>可以折行</li>
</ul>
</li>
</ul>
<pre><code>let a = 12
let str1 = `asdf${a}`
console.log(str1)

let title = '标题'
let content = '内容'
let str = `&lt;div&gt;
&lt;h1&gt;${title}&lt;/h1&gt;
&lt;p&gt;${content}&lt;/p&gt;
`
console.log(str)
&lt;div&gt;
&lt;h1&gt;标题&lt;/h1&gt;
&lt;p&gt;内容&lt;/p&gt;
</code></pre>
<h1 id="面向对象-基础">面向对象-基础</h1>
<ul>
<li>原来写法
<ul>
<li>类和构造函数一样</li>
<li>属性和方法分开写的</li>
</ul>
</li>
</ul>
<pre><code>// 老版本
function User(name, pass) {
    this.name = name
    this.pass = pass
}

User.prototype.showName = function () {
    console.log(this.name)
}
User.prototype.showPass = function () {
    console.log(this.pass)
}

var u1 = new User('able', '1233')
u1.showName()
u1.showPass()
// 老版本继承
function VipUser(name, pass, level) {
    User.call(this, name, pass)
    this.level = level
}
VipUser.prototype = new User()
VipUser.prototype.constructor = VipUser
VipUser.prototype.showLevel = function () {
    console.log(this.level)
}

var v1 = new VipUser('blue', '1234', 3)
v1.showName()
v1.showLevel()
</code></pre>
<ul>
<li>新版面向对象
<ul>
<li>有了 class 关键字、构造器</li>
<li>class 里面直接加方法</li>
<li>继承，super 超类==父类</li>
</ul>
</li>
</ul>
<pre><code>class User {
    constructor(name, pass) {
        this.name = name
        this.pass = pass
    }

    showName() {
        console.log(this.name)
    }
    showPass() {
        console.log(this.pass)
    }
}

var u1 = new User('able2', '111')
u1.showName()
u1.showPass()

// 新版本继承
class VipUser extends User {
    constructor(name, pass, level) {
        super(name, pass)
        this.level = level
    }
    showLevel(){
        console.log(this.level)
    }
}

v1 = new VipUser('blue', '123', 3)
v1.showLevel()
</code></pre>
<h1 id="json">json</h1>
<ul>
<li>JSON 格式
<ul>
<li>JavaScript Object Notation 的缩写，是一种用于数据交换的文本格式</li>
<li>JSON 是 JS对象 的严格子集</li>
<li>JSON 的标准写法</li>
<li>只能用双引号</li>
<li>所有的key都必须用双引号包起来</li>
</ul>
</li>
<li>JSON 对象
<ul>
<li>JSON 对象是 JavaScript 的原生对象，用来处理 JSON 格式数据，有两个静态方法</li>
<li>JSON.parse(string) ：接受一个 <strong>JSON 字符串</strong>并将其转换成一个 JavaScript <strong>对象</strong>。</li>
<li>JSON.stringify(obj) ：接受一个 JavaScript <strong>对象</strong>并将其转换为一个 <strong>JSON 字符串</strong>。</li>
</ul>
</li>
</ul>
<pre><code>var json = {a: 12, b: 5}
var str = 'hi,' + JSON.stringify(json)
var url = 'http://www.xx.com/' + encodeURIComponent(JSON.stringify(json))
console.log(str)
console.log(url)

var str = '{&quot;a&quot;: 12, &quot;b&quot;: 4, &quot;c&quot;: &quot;abc&quot;}'
var json = JSON.parse(str)
console.log(json)
hi,{&quot;a&quot;:12,&quot;b&quot;:5}
http://www.xx.com/%7B%22a%22%3A12%2C%22b%22%3A5%7D
{ a: 12, b: 4, c: 'abc' }
</code></pre>
<ul>
<li>对象（object）
<ul>
<li>是 JavaScript 语言的核心概念，也是最重要的数据类型</li>
<li>对象就是一组“键值对”（key-value）的集合，是一种无序的复合数据集合</li>
<li>对象的所有键名都是字符串, 所以加不加引号都可以</li>
<li>如果键名是数值，会被自动转为字符串</li>
<li>对象的每一个键名又称为“属性”（property），它的“键值”可以是任何数据类型</li>
<li>如果一个属性的值为函数，通常把这个属性称为“方法”，它可以像函数那样调用</li>
<li>in 运算符用于检查对象是否包含某个属性（注意，检查的是键名，不是键值</li>
<li>for...in循环用来遍历一个对象的全部属性</li>
</ul>
</li>
<li>对象 简写
<ul>
<li>key-value 一样时可以简写</li>
<li>里面函数可以简写, 去掉</li>
</ul>
</li>
</ul>
<pre><code>var a = 12, b = 5
console.log({a:a, b:b})
console.log({a, b})
console.log({a, b, c:&quot;c&quot;})
console.log({ a, b, show(){ console.log('a') }})
{ a: 12, b: 5 }
{ a: 12, b: 5 }
{ a: 12, b: 5, c: 'c' }
{ a: 12, b: 5, show: [Function: show] }
</code></pre>
<h2 id=""></h2>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[各类函数]]></title>
        <id>https://merry-9001.github.io/post/ge-lei-han-shu</id>
        <link href="https://merry-9001.github.io/post/ge-lei-han-shu">
        </link>
        <updated>2019-10-24T09:31:55.000Z</updated>
        <content type="html"><![CDATA[<h1>构造函数</h1>
<pre><code>function Person(name , age , gender){
    this.name = name;
    this.age = age;
    this.gender = gender;
    this.sayName = function(){
        alert(this.name);
    };
}
</code></pre>
<p>构造函数就是一个普通的函数，创建方式和普通函数没有区别<br>
<strong>普通函数是直接调用，而构造函数需要使用new关键字来调用</strong></p>
 <h1>回调函数</h1>   
<figure data-type="image" tabindex="1"><img src="https://merry-9001.github.io/post-images/1576910604762.png" alt=""></figure>
<h2 id="如果需要获取一个函数中异步操作的结果则必须通过回调函数来获取">如果需要获取一个函数中异步操作的结果，则必须通过回调函数来获取</h2>
<pre><code>function fn(callback) {
    setTimeout(function() {
        var data = 'hello'
        callback(data)
    }, 1000)
}
fn(function(data) {
    console.log(data)
})
</code></pre>
<p>如果想取到data的数据进行处理，就只能进行回调函数，因为定时器是异步的，函数不会等待执行完毕就直接结束了，只有在调用的时候传递一个函数给里面的定时器才行</p>
<hr>
<p>1). 你定义的<br>
2). 你没有调<br>
3). 但最终它执行了(在某个时刻或某个条件下)</p>
<pre><code>setTimeout(function () { // 定时器回调函数
alert('到点了'+this)
}, 2000)
</code></pre>
<h1>立即执行函数和匿名函数</h1>
<p>函数定义完，立即被调用，这种函数叫做立即执行函数<br>
立即执行函数往往只会执行一次</p>
<pre><code>(function(){
    alert(&quot;我是一个匿名函数~~~&quot;);
})();

(function(a,b){
    console.log(&quot;a = &quot;+a);
    console.log(&quot;b = &quot;+b);
})(123,456);
</code></pre>
<h1>闭包函数</h1>
 当一个嵌套的内部(子)函数引用了嵌套的外部(父)函数的变量(函数)时, 就产生了闭包
<p>函数嵌套<br>
内部函数引用了外部函数的数据(变量/函数)</p>
<pre><code>function fn1 () {
    var a = 2
    var b = 'abc'
    function fn2 () { //执行函数定义就会产生闭包(不用调用内部函数)
    console.log(a)
    }
    // fn2()
}
function showDelay(msg, time) {
    setTimeout(function () {
    alert(msg)
    }, time)
}
</code></pre>
<p>作用</p>
<ol>
<li>
<p>使用函数内部的变量在函数执行完后, 仍然存活在内存中(延长了局部变量的生命周期)</p>
</li>
<li>
<p>让函数外部可以操作(读写)到函数内部的数据(变量/函数)</p>
</li>
<li>
<p>缺点</p>
</li>
</ol>
<ul>
<li>函数执行完后, 函数内的局部变量没有释放, 占用内存时间会变长</li>
<li>容易造成内存泄露</li>
</ul>
<ol start="2">
<li>解决</li>
</ol>
<ul>
<li>能不用闭包就不用</li>
<li>及时释放<br>
f = null //让内部函数成为垃圾对象--&gt;回收闭包</li>
</ul>
<h1>箭头函数</h1>
如果箭头函数不需要参数或需要多个参数，就使用一个圆括号代表参数部分。
<pre><code>var f = () =&gt; 5;
// 等同于
var f = function ()
{ return 5 };

var sum = (num1, num2) =&gt; num1 + num2;
// 等同于
var sum = function(num1, num2) {
return num1 + num2;
</code></pre>
<p>函数体内的this对象，就是定义时所在的对象，而不是使用时所在的对象。</p>
]]></content>
    </entry>
</feed>
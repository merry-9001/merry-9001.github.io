<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://merry-9001.github.io</id>
    <title>看风听歌</title>
    <updated>2021-02-10T06:33:48.293Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://merry-9001.github.io"/>
    <link rel="self" href="https://merry-9001.github.io/atom.xml"/>
    <subtitle>学习是一辈子的事</subtitle>
    <logo>https://merry-9001.github.io/images/avatar.png</logo>
    <icon>https://merry-9001.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 看风听歌</rights>
    <entry>
        <title type="html"><![CDATA[2021]]></title>
        <id>https://merry-9001.github.io/post/da</id>
        <link href="https://merry-9001.github.io/post/da">
        </link>
        <updated>2021-01-22T12:50:09.000Z</updated>
        <content type="html"><![CDATA[<h1 id="新的开始">新的开始</h1>
<p>字体改变</p>
<pre><code>/* 想要div文字变倾斜 加粗 字号设置为16像素 并且 是微软雅黑 */

​       div {

​           /* font-style: italic;

​           font-weight: 700;

​           font-size: 16px;

​           font-family: 'Microsoft yahei'; */

​           /* 复合属性: 简写的方式  节约代码 */

​           /* font: font-style  font-weight  font-size/line-height  font-family; */

​           /* font: italic 700 16px 'Microsoft yahei'; */

​           font: 20px '黑体';

​       }


</code></pre>
<p>缩进</p>
<pre><code>            /* 文本的第一行首行缩进 多少距离  */

​           /* text-indent: 20px; */
</code></pre>
<p>快捷键</p>
<figure data-type="image" tabindex="1"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210123152705457.png" alt="image-20210123152705457" loading="lazy"></figure>
<pre><code>    &lt;!-- .demo$*5 --&gt;
    &lt;!-- 我们想生成的标签里面默认显示几个文字  div{文字}--&gt;
    &lt;!-- div{$}*5 --&gt;
</code></pre>
<figure data-type="image" tabindex="2"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210123153744849.png" alt="image-20210123153744849" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210123155349946.png" alt="image-20210123155349946" loading="lazy"></figure>
<h2 id="背景图片位置">背景图片位置</h2>
<figure data-type="image" tabindex="4"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210123202417512.png" alt="image-20210123202417512" loading="lazy"></figure>
<pre><code> background-repeat: no-repeat;
            /* background-position:  方位名词; */
            /* background-position: center top; */
            /* background-position: right center; */
            /* 如果是方位名词  right center 和 center right 效果是等价的 跟顺序没有关系 */
            /* background-position: center right; */
            /* 此时 水平一定是靠右侧对齐  第二个参数省略 y 轴是 垂直居中显示的 */
            /* background-position: right; */
            /* 此时 第一个参数一定是 top y轴 顶部对齐   第二个参数省略x  轴是 水平居中显示的 */
            background-position: top;
</code></pre>
<h2 id="背景滚动">背景滚动</h2>
<pre><code>            /* 把背景图片固定住 */

​            background-attachment: fixed;
</code></pre>
<h2 id="背景简写">背景简写</h2>
<figure data-type="image" tabindex="5"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210123210613700.png" alt="image-20210123210613700" loading="lazy"></figure>
<p>字体</p>
<pre><code>font: 12px/1.5 'Microsoft YaHei';//大小-行高-字体类型（行高=12px*1.5）
</code></pre>
<h2 id="两个盒子嵌套的塌陷问题">两个盒子嵌套的塌陷问题</h2>
<pre><code>        .father {
            width: 400px;
            height: 400px;
            background-color: purple;
            margin-top: 50px;
            /* border: 1px solid red; */
            /* border: 1px solid transparent; */
            /* padding: 1px; */
            overflow: hidden;
        }
        .son {
            width: 200px;
            height: 200px;
            background-color: pink;
            margin-top: 100px;
        }
</code></pre>
<p>加外边框或者内边框或者overflow: hidden;</p>
<p>塌陷选择大的数字</p>
<h2 id="行内块元素">行内块元素</h2>
<p>/* display: inline-block; */</p>
<p>div之间会存在间隙</p>
<p>如果父盒子设置text-align:center 元素会居中</p>
<h2 id="浮动">浮动</h2>
<p>任何元素都可以浮动。不管原先是什么模式的元素，添加浮动之后具有行内块元素相似的特性</p>
<h3 id="清除浮动方法">清除浮动方法</h3>
<figure data-type="image" tabindex="6"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210124162100287.png" alt="image-20210124162100287" loading="lazy"></figure>
<h2 id="绝对定位">绝对定位</h2>
<figure data-type="image" tabindex="7"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210124164010400.png" alt="image-20210124164010400" loading="lazy"></figure>
<figure data-type="image" tabindex="8"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210124170413568.png" alt="image-20210124170413568" loading="lazy"></figure>
<p>只有定位的盒子才有z-index属性可以设置</p>
<p>相对定位会压住标准流和其他盒子</p>
<p>绝对定位盒子不能居中（相对定位可以）</p>
<figure data-type="image" tabindex="9"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210124170956913.png" alt="image-20210124170956913" loading="lazy"></figure>
<p>​            1.浮动的元素不会压住下面标准流的文字</p>
<p>​            float: left;</p>
<p>​            \2. 绝对定位（固定定位） 会压住下面标准流所有的内容。</p>
<h2 id="display">display</h2>
<figure data-type="image" tabindex="10"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210124214336955.png" alt="image-20210124214336955" loading="lazy"></figure>
<p>隐藏对象 不占有位置</p>
<figure data-type="image" tabindex="11"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210124214604210.png" alt="image-20210124214604210" loading="lazy"></figure>
<p>继续占有位置</p>
<h2 id="vertical-align属性">vertical-align属性</h2>
<figure data-type="image" tabindex="12"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210125211711025.png" alt="image-20210125211711025" loading="lazy"></figure>
<p>属性选择器</p>
<figure data-type="image" tabindex="13"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210126120738648.png" alt="image-20210126120738648" loading="lazy"></figure>
<h2 id="图片模糊">图片模糊</h2>
<pre><code>      /* blur是一个函数 小括号里面数值越大，图片越模糊 注意数值要加px单位 */

​     filter: blur(15px);
</code></pre>
<h2 id="过渡动画">过渡动画</h2>
<figure data-type="image" tabindex="14"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210126153057567.png" alt="image-20210126153057567" loading="lazy"></figure>
<figure data-type="image" tabindex="15"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210126153230713.png" alt="image-20210126153230713" loading="lazy"></figure>
<pre><code>transition: all 0.5s;
</code></pre>
<h2 id="transformtranslatexy">transform:translate(x,y)</h2>
<figure data-type="image" tabindex="16"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210126154607136.png" alt="image-20210126154607136" loading="lazy"></figure>
<p>行内标签没效果</p>
<p>旋转</p>
<figure data-type="image" tabindex="17"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210126155740935.png" alt="image-20210126155740935" loading="lazy"></figure>
<h2 id="透视">透视</h2>
<pre><code>perspective: 500px;
</code></pre>
<p>3D</p>
<figure data-type="image" tabindex="18"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210127152542190.png" alt="image-20210127152542190" loading="lazy"></figure>
<p>perspective是d</p>
<p>translateZ是z   transform: translateZ(0);</p>
<h2 id="移动布局">移动布局</h2>
<figure data-type="image" tabindex="19"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210128143849064.png" alt="image-20210128143849064" loading="lazy"></figure>
<h2 id="flex布局">flex布局</h2>
<figure data-type="image" tabindex="20"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210128154359877.png" alt="image-20210128154359877" loading="lazy"></figure>
<figure data-type="image" tabindex="21"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210128154654751.png" alt="image-20210128154654751" loading="lazy"></figure>
<figure data-type="image" tabindex="22"><img src="C:%5CUsers%5Csix%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210128194219497.png" alt="image-20210128194219497" loading="lazy"></figure>
<h2 id="em与rem">em与rem</h2>
<p>/* 1. em相对于父元素 的字体大小来说的 */</p>
<p>/* 2. rem 相对于 html元素 字体大小来说的 */</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[jquery]]></title>
        <id>https://merry-9001.github.io/post/jquery</id>
        <link href="https://merry-9001.github.io/post/jquery">
        </link>
        <updated>2020-01-21T06:54:33.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li>不能添加多个入口函数（window.onload）,如果添加了多个，后面的会把前面的给覆盖。</li>
<li>原生js的api名字都太长太难记。</li>
<li>原生js有的时候代码冗余。</li>
<li>原生js中有些属性或者方法，有浏览器兼容问题。</li>
<li>原生js容错率比较低，前面的代码出了问题，后面的代码执行不了。</li>
</ul>
<h1 id="jquery的优点">jquery的优点</h1>
<ul>
<li>是可以写多个入口函数的。</li>
<li>jQuery的api名字都容易记忆。</li>
<li>jQuery代码简洁（隐式迭代）。</li>
<li>jQuery帮我们解决了浏览器兼容问题。</li>
<li>容错率较高，前面的代码出来了问题，后面的代码不受影响。</li>
</ul>
<h1 id="入口函数">入口函数</h1>
<pre><code>  $(document).ready(function () {
    console.log(&quot;我又是一个入口函数&quot;);
  });
</code></pre>
<pre><code>$(function () {
});
</code></pre>
<p>更简略的写法</p>
<h1 id="属性设置">属性设置</h1>
<h2 id="设置边框">设置边框</h2>
<pre><code>    $('#btn0ne').click(function () {
      $('div').css('border','1px solid red');
    });

</code></pre>
<h2 id="设置文本">设置文本</h2>
<pre><code>    $('#btnTwo').click(function () {
      $('div').text('我是设置的文本');
    });
</code></pre>
<h2 id="采用链式">采用链式</h2>
<pre><code>$('div').width(100).height(100).css('backgroundColor','red').text('哈哈'); 
</code></pre>
<h2 id="按钮点击">按钮点击</h2>
<pre><code>    $('#getBtn').click(function () {
      console.log($('div').text());
    });
</code></pre>
<p>单击id=getBtn的按钮的事件</p>
<p>会获取到这个标签中所有的文本,包括后代元素的文本.</p>
<p>包含了多个dom元素的jQuery对象,通过text()方法获取文本,会把所有dom元素的文本获取到.</p>
<pre><code>$('div').text('设置的文本'); //隐式迭代.
</code></pre>
<p>传入参数就是设置</p>
<h2 id="设置css样式">设置css样式</h2>
<pre><code>      console.log($('div').css('width'));//'200px'
      //注意：获取样式操作只会返回第一个元素对应的样式值。
</code></pre>
<p>给所有标签设置样式</p>
<pre><code>      $('div').css({
        width: 300,
        'height': '300px',
        'background-color': 'green',
        'border': '10px solid red',
        marginTop:10
      });
      //隐式迭代,把每一个div都设置了同样的样式.
</code></pre>
<h2 id="获取元素">获取元素</h2>
<p>设置选择器</p>
<table>
<thead>
<tr>
<th>基本选择器</th>
<th>和css里面的选择器是一样的</th>
</tr>
</thead>
<tbody>
<tr>
<td>id选择器</td>
<td>$('#id')</td>
</tr>
<tr>
<td>类选择器</td>
<td>$(&quot;.类名&quot;)</td>
</tr>
<tr>
<td>标签选择器</td>
<td>$(&quot;标签名&quot;)</td>
</tr>
<tr>
<td>并集选择器</td>
<td>$('.box,.box2')</td>
</tr>
</tbody>
</table>
<p>获取id为father这个div的所有后代div,以及id为father这个div的所有后代span.</p>
<pre><code>    console.log($('#fahter div , #father span'));
</code></pre>
<p>设置奇数行li标签颜色为天蓝色.</p>
<pre><code>    $('li:odd').css('backgroundColor','skyblue');
</code></pre>
<h2 id="类">类</h2>
<p>​    给id为div1的元素添加类.（多个）</p>
<pre><code> $('#div1').addClass('fontSize30 width200');
</code></pre>
<p>removeClass 移除</p>
<p>hasClass 判断</p>
<p>toggleClass 切换（如果元素有某个类就移除这个类, 如果元素没有这类就添加这个类.）</p>
<h2 id="添加元素">添加元素</h2>
<p>追加节点</p>
<pre><code>    $('#btn1').click(function () {
      var $link = $('&lt;a href=&quot;http://news.baidu.com/&quot;&gt;我是新闻&lt;/a&gt;');
      $('#div1').append($link);
    });
</code></pre>
<pre><code>       $('#j_tbData').html(list.join(&quot;&quot;));
</code></pre>
<p>添加节点</p>
<pre><code>var $liNew = $(&quot;&lt;li&gt;我是新创建的li标签&lt;/li&gt;&quot;);
 $('#ul1').append($liNew);
</code></pre>
<p>元素A.before(元素B); 把元素B插入到元素A的前面,作为兄弟元素添加</p>
<pre><code>      var $divNew = $('&lt;div&gt;我是新建的div&lt;/div&gt;');
      $('#ul1').before($divNew);
</code></pre>
<h2 id="清空元素">清空元素</h2>
<p>1.清空ul.</p>
<pre><code>$('#ul1').empty();
</code></pre>
<p>2.移除某一个元素.</p>
<pre><code>$('#li3').remove(); 
</code></pre>
<p>3.移除ul.但是只能获取li3这个标签.</p>
<pre><code>$('#li3').parent().remove();
</code></pre>
<h2 id="显示和隐藏">显示和隐藏</h2>
<pre><code>        $('#j_btnAddData').click(function () {
            $('#j_formAdd').show();
            $('#j_mask').show();
        });

        $('#j_hideFormAdd').click(function () {
            $('#j_formAdd').hide();
            $('#j_mask').hide();
        });
</code></pre>
<h2 id="获取当前属性">获取当前属性</h2>
<p>获取当前点击的a标签的href属性的值和title属性的值。</p>
<pre><code>var srcValue = $(this).attr('href');
</code></pre>
<h2 id="选中与未选中">选中与未选中</h2>
<p>如果多选框是选中状态返回一个true； 如果多选框是取消选中状态那返回就是一个false</p>
<pre><code> console.log($('#ckb1').prop('checked'));
</code></pre>
<h2 id="迭代">迭代</h2>
<pre><code>    $lis.each(function (index,element) {
      // console.log(index); //每一个li标签的索引
      // console.log(element);//每一个li标签,是一个dom对象.

      $(element).css('opacity',(index+1)/10);
    });
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[react]]></title>
        <id>https://merry-9001.github.io/post/react</id>
        <link href="https://merry-9001.github.io/post/react">
        </link>
        <updated>2020-01-19T07:29:00.000Z</updated>
        <content type="html"><![CDATA[<h1 id="基本概念">基本概念</h1>
<h2 id="引入">引入</h2>
<p>三个文件</p>
<pre><code>  &lt;script type=&quot;text/javascript&quot; src=&quot;../js/react.development.js&quot;&gt;&lt;/script&gt;
  &lt;script type=&quot;text/javascript&quot; src=&quot;../js/react-dom.development.js&quot;&gt;&lt;/script&gt;
  &lt;script type=&quot;text/javascript&quot; src=&quot;../js/babel.min.js&quot;&gt;&lt;/script&gt;
</code></pre>
<h2 id="使用">使用</h2>
<pre><code>  &lt;script type=&quot;text/babel&quot;&gt; //必须声明babel
    // 创建虚拟DOM元素
    const vDom = &lt;h1&gt;Hello React&lt;/h1&gt; // 千万不要加引号
    // 渲染虚拟DOM到页面真实DOM容器中
    ReactDOM.render(vDom, document.getElementById('test'))
  &lt;/script&gt;
</code></pre>
<p>script标签类型设置为text/babel</p>
<p>使用JSX创建虚拟DOM</p>
<h2 id="遍历li">遍历li</h2>
<p>两种方法</p>
<pre><code>var names = ['jquery', 'zeptoo', 'angular', 'react全家桶', 'vue全家桶']
</code></pre>
<p>一</p>
<pre><code>    var lis = []
    names.forEach((name, index) =&gt; lis.push(&lt;li key={index}&gt;{name}&lt;/li&gt;))
    // 创建虚拟DOM
    const ul = &lt;ul&gt;{lis}&lt;/ul&gt;
    // 渲染虚拟DOM
    ReactDOM.render(ul, document.getElementById('example1'))
</code></pre>
<p>二</p>
<pre><code>    const ul2 = &lt;ul&gt;{
      names.map((name, index) =&gt; &lt;li key={index}&gt;{name}&lt;/li&gt;)
    }&lt;/ul&gt;
    ReactDOM.render(ul2, document.getElementById('example2'))
</code></pre>
<h2 id="括号">括号</h2>
<p>{：表示要使用js了</p>
<p>&lt;：表示要使用HTML</p>
<h2 id="定义组件">定义组件</h2>
<p>​    <em>方式1: 工厂函数组件(简单组件)</em>/</p>
<pre><code>    function MyComponent () {

​      return &lt;h2&gt;工厂函数组件(简单组件)&lt;/h2&gt;

​    }
</code></pre>
<p>​    <em>方式2: ES6类组件(复杂组件)</em>/</p>
<pre><code>    class MyComponent2 extends React.Component {

​      render () {

​        console.log(this) // MyComponent2的实例对象

​        return &lt;h2&gt;ES6类组件(复杂组件)&lt;/h2&gt;

​      }

​    }
</code></pre>
<p>渲染组件标签</p>
<pre><code>   ReactDOM.render(&lt;MyComponent /&gt;, document.getElementById('example1'))

​  ReactDOM.render(&lt;MyComponent2 /&gt;, document.getElementById('example2'))
</code></pre>
<h2 id="state">state</h2>
<p>一个标签 点一下进行切换</p>
<pre><code>  class Like extends React.Component {
    constructor (props) {
      super(props)
      // 初始化状态
      this.state = {
        isLikeMe: true
      }
      // 绑定this为组件对象
      this.change = this.change.bind(this)
    }
    change () {
      // 更新状态: this.setState()
      // this.state.isLikeMe = !this.state.isLikeMe // 不能更新更新某个状态
      this.setState({
        isLikeMe: !this.state.isLikeMe
      })
    }
    render () {
      console.log('render()')
      const text = this.state.isLikeMe ? '你喜欢我' : '我喜欢你'
      return &lt;h2 onClick={this.change}&gt;{text}&lt;/h2&gt;
    }
  }
</code></pre>
<p>​</p>
<p>​	是为了改变This指向，这样change函数里面就可以正常使用这个类了，通过this</p>
<pre><code>  this.change = this.change.bind(this)
</code></pre>
<p>必须赋值完以后还要setstate后才有效果</p>
<pre><code>  this.setState({
    isLikeMe: !this.state.isLikeMe
  })
</code></pre>
<p>通过{this.change}（不加括号来绑定事件）</p>
<pre><code>return &lt;h2 onClick={this.change}&gt;{text}&lt;/h2&gt;
</code></pre>
<h2 id="props">props</h2>
<p>往组件内传值</p>
<p>组件</p>
<pre><code>  class Person extends React.Component {
    render() {
      console.log(this)
      return (
        &lt;ul&gt;
          &lt;li&gt;姓名: {this.props.name}&lt;/li&gt;
          &lt;li&gt;性别: {this.props.sex}&lt;/li&gt;
          &lt;li&gt;年龄: {this.props.age}&lt;/li&gt;
        &lt;/ul&gt;
      )
    }
  }
</code></pre>
<pre><code>传值方法一
</code></pre>
<pre><code>  const person = {

​    name: 'Tom',

​    sex: '女',

​    age: 18

  }

  ReactDOM.render(&lt;Person {...person}/&gt;, document.getElementById('example1'))
</code></pre>
<p>传值方法二</p>
<pre><code>  const person2 = {

​    myName: 'JACK',

​    age: 17

  }

  ReactDOM.render(&lt;Person name={person2.myName} age={person2.age}/&gt;,

​    document.getElementById('example2'))
</code></pre>
<p>对标签属性进行限制</p>
<p>Person.propTypes = {</p>
<p>​    name: PropTypes.string.isRequired,</p>
<p>​    sex: PropTypes.string,</p>
<p>​    age: PropTypes.number</p>
<p>}</p>
<p>指定属性的默认值</p>
<p>Person.defaultProps = {</p>
<p>​    sex: '男',</p>
<p>​    age: 18</p>
<p>}</p>
<h2 id="refs">refs</h2>
<p>获取页面中的值</p>
<p>定义组件</p>
<pre><code>  class MyComponent extends React.Component {
      constructor(props) {
        super(props) // 调用父类(Component)的构造函数
        // 将自定义的函数强制绑定为组件对象
        this.handleClick = this.handleClick.bind(this)
      }
      // 自定义的方法中的this默认为null
      handleClick () {
        // alert(this) //this默认是null, 而不是组件对象
        // 得到绑定在当前组件对象上的input的值
        alert(this.msgInput.value)
      }
      handleBlur (event) {
        alert(event.target.value)
      }
      render () {
        return (
          &lt;div&gt;
            &lt;input type=&quot;text&quot; ref={input =&gt; this.msgInput = input}/&gt;{' '}
            &lt;button onClick={this.handleClick}&gt;提示输入数据&lt;/button&gt;{' '}
            &lt;input type=&quot;text&quot; placeholder=&quot;失去焦点提示数据&quot; onBlur={this.handleBlur}/&gt;
          &lt;/div&gt;
        )
      }
    }
</code></pre>
<p><strong>自定义的方法中的this默认为null</strong></p>
<p>取值方式一</p>
<pre><code>&lt;input type=&quot;text&quot; ref={input =&gt; this.msgInput = input}/&gt;
</code></pre>
<p>取值方式二</p>
<pre><code>&lt;input type=&quot;text&quot; placeholder=&quot;失去焦点提示数据&quot; onBlur={this.handleBlur}/&gt;
</code></pre>
<h2 id="综合运用父子组件通信">综合运用（父子组件通信）</h2>
<p>主组件</p>
<pre><code> class App extends React.Component {
      constructor (props) {
        super(props)
        // 初始化状态
        this.state = {
          todos: ['吃饭', '睡觉', '打豆豆']
        }
        this.add = this.add.bind(this)
      }
      add (todo) {
        const {todos} = this.state
        todos.unshift(todo)
        //更新状态
        this.setState({todos})
      }
      render () {
        const {todos} = this.state
        return (
          &lt;div&gt;
            &lt;TodoAdd add={this.add} count={todos.length} /&gt;
            &lt;TodoList todos={todos} /&gt;
          &lt;/div&gt;
        )
      }
    }
</code></pre>
<p>两个子组件</p>
<p>1.传入一个方法，一个长度属性</p>
<p>2.给List数据</p>
<pre><code>        &lt;TodoAdd add={this.add} count={todos.length} /&gt;
        &lt;TodoList todos={todos} /&gt;
</code></pre>
<p>TodoAdd组件</p>
<pre><code>    // 添加todo组件
    class TodoAdd extends React.Component {
      constructor (props) {
        super(props)
        this.addTodo = this.addTodo.bind(this)
      }
      addTodo () {
        // 读取输入数据
        const text = this.input.value.trim()
        // 查检
        if(!text) {
          return
        }
        // 保存到todos
        this.props.add(text)
        // 清除输入
        this.input.value = ''
      }
      render () {
        return (
          &lt;div&gt;
            &lt;h2&gt;Simple TODO List&lt;/h2&gt;
            &lt;input type=&quot;text&quot; ref={input =&gt; this.input=input}/&gt;
            &lt;button onClick={this.addTodo}&gt;Add #{this.props.count}&lt;/button&gt;
          &lt;/div&gt;
        )
      }
    }
</code></pre>
<p>使用父组件的方法</p>
<pre><code>       this.props.add(text)
</code></pre>
<p>使用父组件传来的值</p>
<pre><code>&lt;button onClick={this.addTodo}&gt;Add #{this.props.count}&lt;/button&gt;
</code></pre>
<p>todoList</p>
<pre><code>    class TodoList extends React.Component {
      render () {
        const {todos} = this.props
        return (
          &lt;ul&gt;
            {
              todos.map((todo, index) =&gt; &lt;li key={index}&gt;{todo}&lt;/li&gt;)
            }
          &lt;/ul&gt;
        )
      }
    }
</code></pre>
<p>取到父组件传来的值，然后进行循环输出ui&gt;li</p>
<h2 id="表单上传">表单上传</h2>
<pre><code> class LoginForm extends React.Component {
    constructor(props) {
      super(props)
      this.state = {
        username: ''
      }
      this.handleSubmit = this.handleSubmit.bind(this)
      this.handleChange = this.handleChange.bind(this)
    }

    handleChange(event) {
      this.setState({username: event.target.value})
    }

    handleSubmit(event) {
      alert(`准备提交的用户名为: ${this.state.username}, 密码:${this.pwdInput.value}`)

      // 阻止事件的默认行为: 提交表单
      event.preventDefault()
    }
    render () {

      return (
        &lt;form onSubmit={this.handleSubmit} action=&quot;/test&quot;&gt;
          &lt;label&gt;
            用户名:
            &lt;input type=&quot;text&quot; value={this.state.username} onChange={this.handleChange} /&gt;
          &lt;/label&gt;&amp;nbsp;
          &lt;label&gt;
            密码:
            &lt;input type=&quot;password&quot; ref={(input) =&gt; this.pwdInput = input} /&gt;
          &lt;/label&gt;&amp;nbsp;
          &lt;input type=&quot;submit&quot; value=&quot;登陆&quot; /&gt;
        &lt;/form&gt;
      )
    }
  }
</code></pre>
<p>方法一</p>
<pre><code> &lt;input type=&quot;text&quot; value={this.state.username} onChange={this.handleChange} /&gt;
</code></pre>
<pre><code>  this.state = {
    username: ''
  }
</code></pre>
<p>方法二</p>
<pre><code> &lt;input type=&quot;password&quot; ref={(input) =&gt; this.pwdInput = input} /&gt;
</code></pre>
<p>获取值</p>
<pre><code>handleSubmit(event) {
  alert(`准备提交的用户名为: ${this.state.username}, 密码:${this.pwdInput.value}`)
}
</code></pre>
<p>阻止事件的默认行为: 提交表单</p>
<pre><code>  event.preventDefault()
</code></pre>
<h2 id="生命周期">生命周期</h2>
<figure data-type="image" tabindex="1"><img src="https://merry-9001.github.io/post-images/1579483975928.jpg" alt="" loading="lazy"></figure>
<pre><code>render(): 初始化渲染或更新渲染调用
componentDidMount(): 开启监听, 发送 ajax 请求
componentWillUnmount(): 做一些收尾工作, 如: 清理定时器
</code></pre>
<pre><code>    componentWillMount () {
      console.log('componentWillMount(): 初始化将要挂载')
    }
</code></pre>
<pre><code>    componentDidMount () {// 在此方法中启动定时器/绑定监听/发送ajax请求
      console.log('componentDidMount(): 初始化已经挂载')
      }
</code></pre>
<pre><code>    componentWillUpdate () {
      console.log('componentWillUpdate(): 将要更新')
    }
</code></pre>
<pre><code>    componentDidUpdate () {
      console.log('componentDidUpdate(): 已经更新')
    }
</code></pre>
<pre><code>    componentWillUnmount () {
      console.log('componentWillUnmount(): 将要被移除')
    }
</code></pre>
<pre><code>   removeComponent () {//移除
      
    }
</code></pre>
<pre><code>    render() {
      console.log('render() 渲染组件')
      return (
        &lt;div&gt;
          &lt;h2 style=(两个花括号)opacity:this.state.opacity(两个花括号)&gt;{this.props.content}&lt;/h2&gt;
          &lt;button onClick={this.removeComponent}&gt;不活了&lt;/button&gt;
        &lt;/div&gt;
      )
    }
</code></pre>
<p>style使用state里面的值</p>
<pre><code>&lt;h2 style=(两个花括号)opacity:this.state.opacity(两个花括号)&gt;{this.props.content}&lt;/h2&gt;
</code></pre>
<h2 id="虚拟dom算法">虚拟dom算法</h2>
<figure data-type="image" tabindex="2"><img src="https://merry-9001.github.io/post-images/1579483940062.jpg" alt="" loading="lazy"></figure>
<h2 id="axios">axios</h2>
<p>使用方法跟在vue里面一样</p>
<pre><code> axios.get(url)
        .then(response =&gt; {
          const result = response.data
          console.log(result)
          const repo = result.items[0]
          this.setState({
            repoName: repo.name,
            repoUrl: repo.html_url
          })
        })
        .catch(error =&gt; {
          // debugger
          console.log(error)
          alert('请求失败 '+ error.message)
        })
</code></pre>
<p>字符串里面使用react的变量${this.props.searchWord}</p>
<pre><code>  const url = `https://api.github.com/search/repositories?q=${this.props.searchWord}&amp;sort=stars`
</code></pre>
<h1 id="脚手架">脚手架</h1>
<h2 id="使用-2">使用</h2>
<pre><code>npm install -g create-react-app 安装
create-react-app hello-react  创建
cd hello-react 进入
npm start 运行
</code></pre>
<h2 id="订阅与发布">订阅与发布</h2>
<p><strong>用与父子通信、兄弟组件之间的传值</strong></p>
<p>下载、引用</p>
<figure data-type="image" tabindex="3"><img src="https://merry-9001.github.io/post-images/1579419283659.jpg" alt="" loading="lazy"></figure>
<p>发布</p>
<figure data-type="image" tabindex="4"><img src="https://merry-9001.github.io/post-images/1579419300291.jpg" alt="" loading="lazy"></figure>
<p>订阅</p>
<figure data-type="image" tabindex="5"><img src="https://merry-9001.github.io/post-images/1579419292125.jpg" alt="" loading="lazy"></figure>
<h2 id="router">router</h2>
<h3 id="组件">组件</h3>
<pre><code>&lt;BrowserRouter&gt;
&lt;HashRouter&gt;
&lt;Route&gt;
&lt;Redirect&gt;
&lt;Link&gt;
&lt;NavLink&gt;
&lt;Switch&gt;
</code></pre>
<h3 id="准备">准备</h3>
<pre><code>npm install --save react-router-dom
</code></pre>
<p>解构引用，引用需要的路由组件</p>
<pre><code>import {BrowserRouter, HashRouter} from 'react-router-dom'
</code></pre>
<h3 id="使用-3">使用</h3>
<pre><code>    &lt;BrowserRouter&gt;

​      &lt;App/&gt;

​    &lt;/BrowserRouter&gt;
</code></pre>
<p>app为内容，使用BrowserRouter包起来</p>
<h3 id="路由链接">路由链接</h3>
<pre><code>export default function MyNavLink(props) {
  return &lt;NavLink {...props} activeClassName='activeClass'/&gt;
}
</code></pre>
<p>包装</p>
<pre><code> &lt;MyNavLink className=&quot;list-group-item&quot; to='/about'&gt;About&lt;/MyNavLink&gt;
</code></pre>
<p>使用mynavLink包装了navlink，这样以后就可以有些一定要写的属性就可以放进去了</p>
<h3 id="路由链接跳转">路由链接跳转</h3>
<pre><code>                &lt;Switch&gt;
                  &lt;Route path='/about' component={About}/&gt;
                  &lt;Route path='/home' component={Home}/&gt;
                  &lt;Redirect to='/about'/&gt;
                &lt;/Switch&gt;
</code></pre>
<p>使用switch表示是路由可以切换</p>
<p>route表示 什么链接，跳到什么组件</p>
<p>Redirect表示路由链接都不正确的时候切换成哪个组件</p>
<h3 id="路由嵌套">路由嵌套</h3>
<pre><code>          &lt;li&gt;
            &lt;MyNavLink to='/home/news'&gt;News&lt;/MyNavLink&gt;
          &lt;/li&gt;
          &lt;li&gt;
            &lt;MyNavLink to=&quot;/home/message&quot;&gt;Message&lt;/MyNavLink&gt;
          &lt;/li&gt;
        &lt;/ul&gt;
        &lt;Switch&gt;
          &lt;Route path='/home/news' component={News} /&gt;
          &lt;Route path='/home/message' component={Message} /&gt;
          &lt;Redirect to='/home/news'/&gt;
        &lt;/Switch&gt;
</code></pre>
<p>这部分代码已经是home组件内了，只需要在/home后面再加上需要的路由就行了</p>
<h3 id="路由传递动态id">路由传递动态id</h3>
<ol>
<li>
<p>history 对象</p>
</li>
<li>
<p>match 对象</p>
</li>
<li>
<p>withRouter 函数</p>
</li>
</ol>
<p><strong>切换路由的时候会传递这三个对象</strong></p>
<pre><code>    const path = this.props.match.path
</code></pre>
<p>取到当前的路径</p>
<pre><code>                &lt;li key={index}&gt;
                  &lt;Link to={`${path}/${m.id}`}&gt;{m.title}&lt;/Link&gt;
                  &amp;nbsp;&amp;nbsp;&amp;nbsp;
                  &lt;button onClick={() =&gt; this.ShowDetail(m.id)}&gt;查看详情(push)&lt;/button&gt;&amp;nbsp;
                  &lt;button onClick={() =&gt; this.ShowDetail2(m.id)}&gt;查看详情(replace)&lt;/button&gt;
                &lt;/li&gt;
</code></pre>
<p>切换路径的时候传入Id</p>
<p>router接收传递给组件</p>
<pre><code>&lt;Route path={`${path}/:id`} component={MessageDetail}&gt;&lt;/Route&gt;
</code></pre>
<p>组件接收</p>
<pre><code>  const id = props.match.params.id
</code></pre>
<h2 id="ui库">ui库</h2>
<h3 id="下载">下载</h3>
<pre><code>npm install antd-mobile --save
</code></pre>
<p>蚂蚁金服库</p>
<h3 id="引入-2">引入</h3>
<pre><code>import {Button, Toast} from 'antd-mobile'
</code></pre>
<p>解构按需引入</p>
<p>引入整体css</p>
<pre><code>import 'antd-mobile/dist/antd-mobile.css'
</code></pre>
<p>如果要部分引入css需要安装包插件和修改config设置</p>
<h3 id="使用-4">使用</h3>
<pre><code>&lt;Button type=&quot;primary&quot; onClick={this.handleClick}&gt;提交&lt;/Button&gt;
</code></pre>
<pre><code>  handleClick = () =&gt; {

    Toast.info('提交成功', 2)
  }
</code></pre>
<p>直接使用它的标签就好</p>
<h2 id="redux">redux</h2>
<h3 id="介绍">介绍</h3>
<p>redux 是一个独立专门用于做状态管理的 JS 库(不是 react 插件库)</p>
<p>它可以用在 react, angular, vue 等项目中, 但基本与 react 配合使用</p>
<p>作用: 集中式管理 react 应用中多个组件共享的状态</p>
<h3 id="使用-5">使用</h3>
<figure data-type="image" tabindex="6"><img src="https://merry-9001.github.io/post-images/1579572733863.jpg" alt="" loading="lazy"></figure>
<p>使用redux结构</p>
<p>index.js</p>
<pre><code>import {Provider} from 'react-redux'
import App from './containers/app'
import store from './redux/store'
</code></pre>
<pre><code>ReactDOM.render(
  (
    &lt;Provider store={store}&gt;
      &lt;App/&gt;
    &lt;/Provider&gt;
  ),
  document.getElementById('root')
)
</code></pre>
<p>包裹app 状态进行托管</p>
<hr>
<p>app.jsx（感觉像是桥梁）</p>
<pre><code>import React from 'react'
// 引入连接函数
import {connect} from 'react-redux'
// 引入action函数
import {increment, decrement, incrementAsync} from '../redux/actions'
import Counter from '../components/counter'

// 向外暴露连接App组件的包装组件
export default connect(
  state =&gt; ({count: state.counter}),
  {increment, decrement, incrementAsync}
)(Counter)
</code></pre>
<p>将redux定义的函数以及值传递给组件</p>
<p>是从redux的actions.js中去取</p>
<hr>
<p>counter.jsx组件值和函数的使用</p>
<pre><code>import PropTypes from 'prop-types'
</code></pre>
<pre><code>  static propTypes = {
    count: PropTypes.number.isRequired,
    increment: PropTypes.func.isRequired,
    decrement: PropTypes.func.isRequired
  }
</code></pre>
<p>使用</p>
<pre><code>    this.props.increment(number)//函数调用
</code></pre>
<pre><code>&lt;button onClick={this.increment}&gt;+&lt;/button&gt;{' '}
</code></pre>
<hr>
<p>redux内部</p>
<p>action-types.js</p>
<pre><code>export const INCREMENT = 'increment'
export const DECREMENT = 'decrement'
</code></pre>
<p>定义常量进行使用</p>
<hr>
<p>actions.js</p>
<pre><code>import {INCREMENT,DECREMENT} from './action-types'
</code></pre>
<pre><code>export const increment = number =&gt; ({type: INCREMENT, number})

export const decrement = number =&gt; ({type: DECREMENT, number})
</code></pre>
<p>引用之前定义的常量</p>
<p>创建函数 确定函数名，参数和返回值</p>
<hr>
<p>reducers.js</p>
<pre><code>import {combineReducers} from 'redux'
</code></pre>
<p>引入，这样可以向外暴露多个函数</p>
<pre><code>import {INCREMENT,DECREMENT} from './action-types'
</code></pre>
<p>引入常量</p>
<pre><code>function counter(state = 0, action) {
  console.log('counter', state, action)
  switch (action.type) {
    case INCREMENT:
      return state + action.number
    case DECREMENT:
      return state - action.number
    default:
      return state
  }
}

</code></pre>
<p>使用combineReducers可以是多个参数</p>
<pre><code>export default combineReducers({
  counter
})
</code></pre>
<hr>
<p>store.js</p>
<p>主要文件,index.js引入</p>
<pre><code>import reducers from './reducers'
</code></pre>
<p>引入</p>
<pre><code>import {composeWithDevTools} from 'redux-devtools-extension'
</code></pre>
<p>调试用</p>
<p>创建方法</p>
<pre><code>import React from 'react'
import {createStore, applyMiddleware} from 'redux'
import thunk from 'redux-thunk'

// 根据counter函数创建store对象
export default createStore(
  reducers,
  composeWithDevTools(applyMiddleware(thunk)) // 应用上异步中间件
)
</code></pre>
<h3 id="异步方法">异步方法</h3>
<pre><code>// 异步action creator(返回一个函数)
export const incrementAsync = number =&gt; {
  return dispatch =&gt; {
    setTimeout(() =&gt; {
      dispatch(increment(number))
    }, 1000)
  }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[hmtl-css]]></title>
        <id>https://merry-9001.github.io/post/hmtl-css</id>
        <link href="https://merry-9001.github.io/post/hmtl-css">
        </link>
        <updated>2020-01-06T06:36:58.000Z</updated>
        <content type="html"><![CDATA[<h1 id="html">html</h1>
<h2 id="meta">meta</h2>
<p><strong>meta主要用于设置网页中的一些元数据，元数据不是给用户看</strong></p>
<p>​            charset 指定网页的字符集</p>
<p>​            name 指定的数据的名称</p>
<p>​            content 指定的数据的内容</p>
<pre><code>     &lt;meta name=&quot;keywords&quot; content=&quot;HTML5,前端,CSS3&quot;&gt;
     &lt;meta name=&quot;description&quot; content=&quot;这是一个非常不错的网站&quot;&gt;
</code></pre>
<p>name的可选项</p>
<ul>
<li>description 用于指定网站的描述</li>
<li>keywords 表示网站的关键字，可以同时指定多个关键字，关键字间使用,隔开</li>
</ul>
<h2 id="section">section</h2>
<p>section 表示一个独立的区块</p>
<h2 id="列表list">列表list</h2>
<ul>
<li>无序列表，使用ul标签来创建无序列表</li>
</ul>
<p>​        使用li表示列表项</p>
<pre><code>    &lt;ul&gt;

​        &lt;li&gt;结构&lt;/li&gt;

​        &lt;li&gt;表现&lt;/li&gt;


​    &lt;/ul&gt;
</code></pre>
<ul>
<li>​    无序列表，使用ol标签来创建无序列表</li>
</ul>
<p>​        使用li表示列表项</p>
<pre><code>    &lt;ol&gt;

​        &lt;li&gt;结构&lt;/li&gt;

​        &lt;li&gt;表现&lt;/li&gt;


​    &lt;/ol&gt;
</code></pre>
<ul>
<li>​    定义列表，使用dl标签来创建一个定义列表</li>
</ul>
<p>​        使用dt来表示定义的内容</p>
<p>​        使用dd来对内容进行解释说明</p>
<pre><code>    &lt;dl&gt;

​        &lt;dt&gt;结构&lt;/dt&gt;

​        &lt;dd&gt;结构表示网页的结构，结构用来规定网页中哪里是标题，哪里是段落&lt;/dd&gt;

​        &lt;dd&gt;结构表示网页的结构，结构用来规定网页中哪里是标题，哪里是段落&lt;/dd&gt;


​    &lt;/dl&gt;
</code></pre>
<h1 id="css">css</h1>
<h2 id="样式继承">样式继承</h2>
<p>样式的继承，我们为一个元素设置的样式同时也会应用到它的后代元素上</p>
<p><strong>背景相关的，布局相关等的这些样式都不会被继承。</strong></p>
<h2 id="优先级">优先级</h2>
<p>​            <strong>选择器的权重</strong></p>
<p>​                内联样式             1,0,0,0</p>
<p>​                id选择器              0,1,0,0</p>
<p>​                类和伪类选择器   0,0,1,0</p>
<p>​                元素选择器          0,0,0,1</p>
<p>​                通配选择器          0,0,0,0</p>
<p>​                继承的样式         没有优先级</p>
<h2 id="长度单位">长度单位</h2>
<ul>
<li>像素</li>
</ul>
<p>​       - 屏幕（显示器）实际上是由一个一个的小点点构成的</p>
<p>​       - 不同屏幕的像素大小是不同的，像素越小的屏幕显示的效果越清晰</p>
<p>​       - 所以同样的200px在不同的设备下显示效果不一样</p>
<ul>
<li>百分比</li>
</ul>
<p>​       - 也可以将属性值设置为相对于其父元素属性的百分比</p>
<p>​       - 设置百分比可以使子元素跟随父元素的改变而改变</p>
<ul>
<li>em</li>
</ul>
<p>​      - em是相对于元素的字体大小来计算的</p>
<p>​      - 1em = 1font-size</p>
<p>​      - em会根据字体大小的改变而改变</p>
<ul>
<li>rem</li>
</ul>
<p>​      - rem是相对于根元素的字体大小来计算</p>
<h2 id="文档流">文档流</h2>
<p>​            - 网页是一个多层的结构，一层摞着一层</p>
<p>​            - 作为用户来讲只能看到最顶上一层</p>
<p>​            - 这些层中，最底下的一层称为文档流，文档流是网页的基础</p>
<p>​                我们所创建的元素默认都是在文档流中进行排列</p>
<p>​            - 对于我们来元素主要有两个状态</p>
<p>​                在文档流中</p>
<p>​                不在文档流中（脱离文档流）</p>
<h2 id="块级元素">块级元素</h2>
<p>​                    - 块元素会在页面中独占一行(自上向下垂直排列)</p>
<p>​                    - 默认宽度是父元素的全部（会把父元素撑满）</p>
<p>​                    - 默认高度是被内容撑开（子元素）</p>
<pre><code>&lt;div&gt;、&lt;p&gt;、&lt;h1&gt;...&lt;h6&gt;、&lt;ol&gt;、&lt;ul&gt;、&lt;dl&gt;、&lt;table&gt;、&lt;address&gt;、&lt;blockquote&gt; 、&lt;form&gt;#
</code></pre>
<h2 id="行内元素">行内元素</h2>
<p>​                    - 行内元素不会独占页面的一行，只占自身的大小</p>
<p>​                    - 行内元素的默认宽度和高度都是被内容撑开</p>
<pre><code>&lt;span&gt;...&lt;/span&gt;

&lt;a&gt;...&lt;/a&gt;  链接

&lt;br&gt;  换行

&lt;b&gt;...&lt;/b&gt;  加粗

&lt;strong&gt;...&lt;/strong&gt;  加粗

&lt;img &gt;  图片
</code></pre>
<p>​                    - 行内元素不支持设置宽度和高度</p>
<p>​                    - 行内元素可以设置padding，但是垂直方向padding不会影响页面的布局</p>
<p>​                    - 行内元素可以设置border，垂直方向的border不会影响页面的布局</p>
<p>​                    - 行内元素可以设置margin，垂直方向的margin不会影响布局</p>
<h2 id="overflow">overflow</h2>
<ul>
<li>​                子元素是在父元素的内容区中排列的，</li>
</ul>
<p>​                    如果子元素的大小超过了父元素，则子元素会从父元素中溢出</p>
<p>​                    使用 overflow 属性来设置父元素如何处理溢出的子元素</p>
<ul>
<li>​                    可选值：</li>
</ul>
<p>​                        visible，默认值 子元素会从父元素中溢出，在父元素外部的位置显示</p>
<p>​                        hidden 溢出内容将会被裁剪不会显示</p>
<p>​                        scroll 生成两个滚动条，通过滚动条来查看完整的内容</p>
<p>​                        auto 根据需要生成滚动条</p>
<h2 id="浮动布局方式一">浮动（布局方式一）</h2>
<p><strong>现在有flex了，可以替代float</strong></p>
<p>元素设置浮动以后，会完全从文档流中脱离，不再占用文档流的位置，</p>
<p>所以元素下边的还在文档流中的元素会自动向上移动</p>
<h3 id="浮动的特点">浮动的特点</h3>
<p>1、浮动元素会完全脱离文档流，不再占据文档流中的位置</p>
<p>2、设置浮动以后元素会向父元素的左侧或右侧移动，</p>
<p>3、浮动元素默认不会从父元素中移出</p>
<p>4、浮动元素向左或向右移动时，不会超过它前边的其他浮动元素</p>
<p>5、如果浮动元素的上边是一个没有浮动的块元素，则浮动元素无法上移</p>
<p>6、浮动元素不会超过它上边的浮动的兄弟元素，最多最多就是和它一样高</p>
<p>通过浮动可以制作一些水平方向的布局</p>
<p><strong>浮动就是脱离文档流了，叠在文档流上面，在文档流里面的数据就会覆盖在他的下面，原来是原来将文字环绕的，文字不会叠在他的下面，而是环绕排列</strong></p>
<p><strong>脱离文档流以后，不需要再区分块和行内了 直接变成块元素</strong></p>
<h2 id="bfc块级格式化环境">BFC:块级格式化环境</h2>
<h3 id="高度塌陷">高度塌陷</h3>
<p>在浮动布局中，父元素的高度默认是被子元素撑开的，</p>
<p>当子元素浮动后，其会完全脱离文档流，子元素从文档流中脱离</p>
<p>将会无法撑起父元素的高度，导致父元素的高度丢失</p>
<p>父元素高度丢失以后，其下的元素会自动上移，导致页面的布局混乱</p>
<h3 id="介绍">介绍</h3>
<p>BFC(Block Formatting Context) 块级格式化环境</p>
<p>开启BFC该元素会变成一个独立的布局区域</p>
<h3 id="特点">特点</h3>
<p>1.开启BFC的元素不会被浮动元素所覆盖</p>
<p>2.开启BFC的元素子元素和父元素外边距不会重叠</p>
<p>3.开启BFC的元素可以包含浮动的子元素</p>
<h3 id="开启方式">开启方式</h3>
<p>1、设置元素的浮动（不推荐）</p>
<p>2、将元素设置为行内块元素（不推荐）</p>
<p>3、将元素的overflow设置为一个非visible的值</p>
<p>- 常用的方式 为元素设置 overflow:hidden 开启其BFC 以使其可以包含浮动元素</p>
<h2 id="清除浮动">清除浮动</h2>
<p>如果我们不希望某个元素因为其他元素浮动的影响而改变位置，</p>
<p>可以通过clear属性来清除浮动元素对当前元素所产生的影响</p>
<p><strong>因为如果前面的浮动的话，这个元素进入到原来浮动的元素上，所以如果不想这么做，就必须清除浮动</strong>，设置这个元素的属性</p>
<p>​                clear</p>
<p>​                    - 作用：清除浮动元素对当前元素所产生的影响</p>
<p>​                    - 可选值：</p>
<p>​                        left 清除左侧浮动元素对当前元素的影响</p>
<p>​                        right 清除右侧浮动元素对当前元素的影响</p>
<p>​                        both 清除两侧中最大影响的那侧</p>
<h2 id="高度塌陷解决方案">高度塌陷解决方案</h2>
<pre><code>        .clearfix::before,
        .clearfix::after{
            content: '';
            display: table;
            clear: both;
        }
</code></pre>
<p>这个既可以解决高度塌陷的问题，也可以解决父元素和子元素的margin-top重合的问题</p>
<p>clearfix 这个样式可以同时解决高度塌陷和外边距重叠的问题，当你在遇到这些问题时，直接使用clearfix这个类即可</p>
<h2 id="定位布局方式二">定位（布局方式二）</h2>
<p>使用position属性来设置定位</p>
<p>可选值：</p>
<ul>
<li>static 默认值，元素是静止的没有开启定位</li>
<li>relative 开启元素的相对定位</li>
<li>absolute 开启元素的绝对定位</li>
<li>fixed 开启元素的固定定位</li>
<li>sticky 开启元素的粘滞定位</li>
</ul>
<h3 id="相对定位relative">相对定位（relative ）</h3>
<ul>
<li>
<p>相对定位是参照于元素在文档流中的位置进行定位的</p>
</li>
<li>
<p>相对定位会提升元素的层级</p>
</li>
<li>
<p>相对定位不会使元素脱离文档流</p>
</li>
<li>
<p>相对定位不会改变元素的性质块还是块，行内还是行内</p>
<p>偏移量</p>
<p>top</p>
<p>- 定位元素和定位位置上边的距离</p>
<p>bottom</p>
<p>- 定位元素和定位位置下边的距离</p>
<p>left</p>
<p>- 定位元素和定位位置的左侧距离</p>
<p>right</p>
<p>- 定位元素和定位位置的右侧距离</p>
</li>
</ul>
<h3 id="绝对定位absolute">绝对定位（absolute）</h3>
<p>2.开启绝对定位后，元素会从文档流中脱离</p>
<p><strong>3.绝对定位会改变元素的性质，行内变成块，块的宽高被内容撑开</strong></p>
<p>4.绝对定位会使元素提升一个层级</p>
<p>5.绝对定位元素是相对于其包含块进行定位的</p>
<p><strong>偏移量等同于相对定位的属性</strong></p>
<h3 id="固定定位fixed">固定定位（fixed）</h3>
<p>固定定位永远参照于浏览器的视口进行定位</p>
<p>固定定位的元素不会随网页的滚动条滚动</p>
<p><strong>偏移量等同于相对定位的属性</strong></p>
<h2 id="元素层级">元素层级</h2>
<p>对于开启了定位元素，可以通过z-index属性来指定元素的层级</p>
<p>元素的层级越高越优先显示</p>
<p>祖先的元素的层级再高也不会盖住后代元素</p>
<pre><code> z-index: 3; 
</code></pre>
<h2 id="文本">文本</h2>
<h3 id="text-align-文本的水平对齐">text-align 文本的水平对齐</h3>
<p>可选值：</p>
<p>left 左侧对齐</p>
<p>right 右对齐</p>
<p>center 居中对齐</p>
<p>justify 两端对齐</p>
<pre><code>text-align: justify; 
</code></pre>
<p>无论是单行还是多行都是一样 两端对齐就是会把中间的空隙给放大</p>
<h3 id="vertical-align-设置元素垂直对齐的方式">vertical-align 设置元素垂直对齐的方式</h3>
<p>可选值：</p>
<p>baseline 默认值 基线对齐</p>
<p>top 顶部对齐</p>
<p>bottom 底部对齐</p>
<p>middle 居中对齐</p>
<pre><code>vertical-align:baseline; 
</code></pre>
<p>Middle居中对齐并不会真的居中而是父元素字母x的对齐</p>
<p>基线对齐就是根据根据父元素的字母的基线一样</p>
<h3 id="引入图片空白">引入图片空白</h3>
<pre><code>    &lt;p&gt;
       &lt;img src=&quot;./img/an.jpg&quot; alt=&quot;&quot;&gt; 
    &lt;/p&gt;
</code></pre>
<p>把Img放入P中的时候默认是基线对齐，所以下面会留空白</p>
<p>将img设置一下垂直对齐的方式就可以解决这个问题</p>
<pre><code>    vertical-align: bottom;
</code></pre>
<h3 id="text-decoration">text-decoration</h3>
<p>可选值：</p>
<p>none 什么都没有</p>
<p>underline 下划线</p>
<p>line-through 删除线</p>
<p>overline 上划线</p>
<h3 id="文字太多显示省略号">文字太多显示省略号</h3>
<pre><code>.box2{

    width: 200px;

    /* 

    white-space 设置网页如何处理空白

    可选值：

    normal 正常

    nowrap 不换行

    pre 保留空白


    */

    white-space: nowrap;  

    overflow: hidden;

    text-overflow: ellipsis;

}
</code></pre>
<p>不换行   多余裁剪    文字溢出使用省略号</p>
<h2 id="字体">字体</h2>
<h3 id="字体-2">字体</h3>
<ul>
<li>color 用来设置字体颜色</li>
<li>font-size 字体的大小</li>
</ul>
<p>​	em 相当于当前元素的一个font-size</p>
<p>​	rem 相对于根元素的一个font-size</p>
<ul>
<li>font-family 字体族（字体的格式）</li>
</ul>
<p>可选值：</p>
<p>​	serif  衬线字体</p>
<p>​	sans-serif 非衬线字体</p>
<p>​	monospace 等宽字体</p>
<p>- 指定字体的类别，浏览器会自动使用该类别下的字体</p>
<p>- font-family 可以同时指定多个字体，多个字体间使用,隔开</p>
<p>字体生效时优先使用第一个，第一个无法使用则使用第二个 以此类推</p>
<pre><code>font-family: 'Courier New', Courier, monospace;
</code></pre>
<p>这些字体都是必须用户电脑里装着的，也可以服务器去给用户指定</p>
<pre><code>        @font-face {

​            /* 指定字体的名字 */

​            font-family:'myfont' ;

​            /* 服务器中字体的路径 */

​            src: url('./font/ZCOOLKuaiLe-Regular.ttf') format(&quot;truetype&quot;);

​        }
</code></pre>
<p>format(&quot;truetype&quot;);是指定类型，是一个保险</p>
<p>这样就是自定义一个字体可以在 font-family里面去使用</p>
<h3 id="行高">行高</h3>
<p>行高指的是文字占有的实际高度</p>
<p>可以将行高设置为和高度一样的值，使单行文字在一个元素中垂直居中</p>
<p>line-height来设置行高</p>
<p>行高可以直接指定一个大小（px em）</p>
<p>也可以直接为行高设置一个整数</p>
<p>如果是一个整数的话，行高将会是字体的指定的倍数</p>
<p>- 行高经常还用来设置文字的行间距（如果是多行字体的话就是用来设置文字的行间距的）</p>
<p>行间距 = 行高 - 字体大小</p>
<h3 id="字体的用法与简写">字体的用法与简写</h3>
<p>font 可以设置字体相关的所有属性</p>
<p>语法：</p>
<p>font: 字体大小/行高 字体族（<strong>字体大小和字体族是必填</strong>）</p>
<p>行高 可以省略不写 如果不写使用默认值</p>
<pre><code>font: bold italic 50px/2  微软雅黑, 'Times New Roman', Times, serif;
</code></pre>
<p><strong>加组 斜体 字体大小/行高  字体族</strong></p>
<p>font-weight 字体的加粗</p>
<p>可选值：</p>
<p>normal 默认值 不加粗</p>
<p>bold 加粗</p>
<p>100-900 九个级别（没什么用）</p>
<pre><code>font-weight: bold; 
</code></pre>
<p>font-style 字体的风格</p>
<p>normal 正常的</p>
<p>italic 斜体</p>
<pre><code>font-style: italic;
</code></pre>
<h3 id="字体图标">字体图标</h3>
<p>在网页中经常需要使用一些图标，可以通过图片来引入图标</p>
<p>但是图片大小本身比较大，并且非常的不灵活</p>
<p>所以在使用图标时，我们还可以将图标直接设置为字体，</p>
<p>然后通过font-face的形式来对字体进行引入</p>
<p>这样我们就可以通过使用字体的形式来使用图标</p>
<h3 id="使用fontawesome">使用fontawesome</h3>
<p>1.下载 https://fontawesome.com/</p>
<p>2.解压</p>
<p>3.将css和webfonts移动到项目中</p>
<p>4.将all.css引入到网页中</p>
<p>5.使用图标字体</p>
<p>class=&quot;fas fa-bell&quot;</p>
<p>class=&quot;fab fa-accessible-icon&quot;</p>
<p>使用方法一</p>
<pre><code>    &lt;i class=&quot;fas fa-bell&quot; style=&quot;font-size:80px; color:red;&quot;&gt;&lt;/i&gt;
</code></pre>
<p>使用方法二</p>
<pre><code>        li::before{
            /* 
                通过伪元素来设置图标字体
                    1.找到要设置图标的元素通过before或after选中
                    2.在content中设置字体的编码
                    3.设置字体的样式
                        fab
                        font-family: 'Font Awesome 5 Brands';

                        fas
                        font-family: 'Font Awesome 5 Free';
                        font-weight: 900; 

            */
            content: '\f1b0';
            /* font-family: 'Font Awesome 5 Brands'; */
            font-family: 'Font Awesome 5 Free';
            font-weight: 900; 
            color: blue;
            margin-right: 10px;
        }
</code></pre>
<p>使用方法三</p>
<pre><code>  &lt;span class=&quot;fas&quot;&gt;&amp;#xf0f3;&lt;/span&gt;
</code></pre>
<p>加入&amp;#x</p>
<h3 id="阿里图标">阿里图标</h3>
<p>https://www.iconfont.cn/</p>
<p>选择要的图表加入购物车</p>
<p>然后下载到本地</p>
<p>将所有文件都放入</p>
<p>引入css即可使用</p>
<h2 id="less">less</h2>
<h3 id="介绍-2">介绍</h3>
<p>less是一个css的增强版，通过less可以编写更少的代码实现更强大的样式</p>
<p>在less中添加了许多的新特性：像对变量的支持、对mixin的支持... ...</p>
<p>less的语法大体上和css语法一致，但是less中增添了许多对css的扩展，</p>
<p>所以浏览器无法直接执行less代码，要执行必须向将less转换为css，然后再由浏览器执行</p>
<h3 id="语法">语法</h3>
<h3 id="可以嵌套">可以嵌套</h3>
<pre><code>.box1{
    background-color: #bfa;

    .box2{
        background-color: #ff0;

        .box4{
            color: red;
        }
    }

    .box3{
        background-color: orange;
    }
}
</code></pre>
<h3 id="可以使用变量">可以使用变量</h3>
<pre><code>//变量，在变量中可以存储一个任意的值
// 并且我们可以在需要时，任意的修改变量中的值
// 变量的语法： @变量名
@a:200px;
@b:#bfa;
@c:box6;

.box5{
    //使用变量是，如果是直接使用则以 @变量名 的形式使用即可
    width: @a;
    color:@b;
}

//作为类名，或者一部分值使用时必须以 @{变量名} 的形式使用
.@{c}{
    width: @a;
    background-image: url(&quot;@{c}/1.png&quot;);
}
</code></pre>
<h3 id="表示当前层的用法">表示当前层的用法</h3>
<pre><code>.box1{
    //为box1设置一个hover
    //&amp; 就表示外层的父元素
    &amp;:hover{
        color: orange;
    }
    div &amp;{
        width: 100px;
    }
}
</code></pre>
<h3 id="继承">继承</h3>
<p>//:extend() 对当前选择器扩展指定选择器的样式（选择器分组）</p>
<p>.p2:extend(.p1){</p>
<p>​    color: red;</p>
<p>}</p>
<h3 id="混合">混合</h3>
<pre><code>// 使用类选择器时可以在选择器后边添加一个括号，这时我们实际上就创建了一个mixins
.p4(){
    width: 100px;
    height: 100px;
}

.p5{
    .p4;
}
</code></pre>
<h3 id="混合函数">混合函数</h3>
<pre><code>//混合函数 在混合函数中可以直接设置变量
.test(@w:100px,@h:200px,@bg-color:red){
    width: @w;
    height: @h;
    border: 1px solid @bg-color;
}
div{
    //调用混合函数，按顺序传递参数
    // .test(200px,300px,#bfa);
    .test(300px);
    // .test(@bg-color:red, @h:100px, @w:300px);
}
</code></pre>
<p>就是说可以定义一个函数，然后这个函数里面也可以有默认值，这样的话，使用的时候不传值也能使用。</p>
<h3 id="运算和引入">运算和引入</h3>
<pre><code>@import &quot;syntax2.less&quot;;

.box1{

​    // 在less中所有的数值都可以直接进行运算

​    // + - * /

​    width: 100px + 100px;

​    height: 100px/2;

​    background-color: #bfa;


}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[tp5]]></title>
        <id>https://merry-9001.github.io/post/tp5</id>
        <link href="https://merry-9001.github.io/post/tp5">
        </link>
        <updated>2019-12-24T11:22:08.000Z</updated>
        <content type="html"><![CDATA[<h1 id="tp5">tp5</h1>
<h2 id="路径">路径</h2>
<p>http://localhost:8000/library/public/admin/users/index.html</p>
<pre><code>admin/users/index.html
</code></pre>
<p>admin下的控制器里面的User.php里面的方法名</p>
<pre><code>public function index()
{

		$selectResult=Db::table('book_user')//热点精选
			-&gt;join('book_role','book_user.role_id = 				book_role.role_id')
		    -&gt;select();
		$this-&gt;assign('user',$selectResult);
	//	return dump($selectResult);
   return $this-&gt;fetch('index');
}
</code></pre>
<h2 id="前台传入后台值通过form">前台传入后台值（通过form）</h2>
<form class="form-auth-small" method="post" action="{:Url('admin/index/login')}">
<pre><code>&lt;form class=&quot;form-auth-small&quot; method=&quot;post&quot; action={:Url('admin/index/login')}&quot;&gt;
</code></pre>
<pre><code>if(request()-&gt;isPost())

</code></pre>
<p>控制器用来判断是否是form表单提交过来的请求 如果是的话会走if</p>
<h2 id="返回前台代码">返回前台代码</h2>
<pre><code> return $this-&gt;fetch('login');
</code></pre>
<h2 id="获取表单数据">获取表单数据</h2>
<pre><code> $data = input(&quot;post.&quot;);
</code></pre>
<p>这样是获取到所有数据</p>
<pre><code>// return dump($data[&quot;name&quot;]);
</code></pre>
<p>这样可以获取到每一条的数据</p>
<h2 id="调试打印">调试打印</h2>
<hr>
<pre><code>    return dump($data);
</code></pre>
<h2 id="database">database</h2>
<p>application的下database用来操作数据库</p>
<pre><code>'type'            =&gt; 'mysql',
// 服务器地址
'hostname'        =&gt; 'usxjsj.cn',
// 数据库名
'database'        =&gt; 'db16147230',
// 用户名
'username'        =&gt; 'u16147230',
// 密码
'password'        =&gt; 'xg1511',
</code></pre>
<h2 id="数据库操作">数据库操作</h2>
<h3 id="增">增</h3>
<pre><code>				   if($data['type1']=='学生')
				   {

					Db::name(&quot;book_admin&quot;)-&gt;insert([
						'admin_account'=&gt;$data['name'],
						'admin_password'=&gt;$data['password'], 
						]
					);
</code></pre>
<p>name(表名)</p>
<p>'admin_account'=&gt;$data['name'],</p>
<p>表里面的字段名=&gt;插入的名字</p>
<h3 id="改">改</h3>
<pre><code>$res` `= Db::name(``'style'``)-&gt;where(``'id'``,4)-&gt;update([``'name_cn'``=&gt;``'李四'``]);
</code></pre>
<p>where(数据库字段名,（值）)逗号表示等于</p>
<h3 id="查">查</h3>
<pre><code>				$s=Db::table('book_borrow')//热点精选
				-&gt;where('book_state',1)
				 -&gt;select();
</code></pre>
<p>$s拿到的就是叫table的表名里面的数据 ,where里面写条件</p>
<pre><code>			$s=Db::table('book_borrow')//热点精选
			-&gt;join('book_info','book_book.book_id = book_info.book_id')
			//-&gt;whereTime('drop_litter_time', 'month')
			-&gt;where('book_state',1)
			 -&gt;select();
</code></pre>
<p>$s拿到的就是叫table的表名里面的数据 ,where里面写条件,join用来连接表，要查两张表里面的接起来的内容</p>
<h2 id=""></h2>
<h3 id="删">删</h3>
<pre><code>$res = Db::name('style')-&gt;where('id',2)-&gt;delete();
</code></pre>
<p>按照条件删除</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[正则表达式]]></title>
        <id>https://merry-9001.github.io/post/zheng-ze-biao-da-shi</id>
        <link href="https://merry-9001.github.io/post/zheng-ze-biao-da-shi">
        </link>
        <updated>2019-12-23T08:29:10.000Z</updated>
        <content type="html"><![CDATA[<h1 id="正则表达式">正则表达式</h1>
<h2 id="量词">量词</h2>
<p>通过量词可以设置一个内容出现的次数</p>
<p>量词只对它前边的一个内容起作用</p>
<pre><code>  {n} 正好出现n次

  {m,n} 出现m-n次

  {m,} m次以上

  + 至少一个，相当于{1,}

  * 0个或多个，相当于{0,}

  ? 0个或1个，相当于{0,1}
</code></pre>
<h2 id="开头结尾">开头结尾</h2>
<p>^ 表示开头</p>
<p>$ 表示结尾</p>
<p>如果在正则表达式中同时使用^ $则要求字符串必须完全符合正则表达式</p>
<pre><code>reg = /^a$/;
</code></pre>
<h2 id="手机号检测">手机号检测</h2>
<p>检查一个字符串是否是一个合法手机号</p>
<pre><code>var phoneReg = /^1[3-9][0-9]{9}$/;

console.log(phoneReg.test(phoneStr));
</code></pre>
<p>以1开头</p>
<p>第二位3-9任意数字</p>
<p>三位以后任意数字9个</p>
<h2 id="通配">通配</h2>
<pre><code>\w
任意字母、数字、_  [A-z0-9_]
\W
除了字母、数字、_  [^A-z0-9_]
\d
任意的数字 [0-9]
\D
除了数字 [^0-9]
\s
空格
\S
除了空格
\b
单词边界
\B
除了单词边界
</code></pre>
<h2 id="邮件正则">邮件正则</h2>
<pre><code>var emailReg = /^\w{3,}(\.\w+)*@[A-z0-9]+(\.[A-z]{2,5}){1,2}$/;

var email = &quot;739.211562@qq.com&quot;;
</code></pre>
<p>任意字母数字下划线    .任意字母数字下划线  @   任意字母数字     .任意字母（2-5位）   .任意字母（2-5位）</p>
<pre><code>\w{3,}  (\.\w+)*  @  [A-z0-9]+  (\.[A-z]{2,5}){1,2}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[markdown标记语言]]></title>
        <id>https://merry-9001.github.io/post/markdown-biao-ji-yu-yan</id>
        <link href="https://merry-9001.github.io/post/markdown-biao-ji-yu-yan">
        </link>
        <updated>2019-11-08T13:28:47.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li>HTML 也是标记语言</li>
<li>markdown 标记语言</li>
</ul>
<p>优点：<br>
1、因为是纯文本，所以只要支持Markdown的地方都能获得一样的编辑效果，可以让作者摆脱排版的困扰，专心写作。<br>
2、操作简单。比如:WYSIWYG编辑时标记个标题，先选中内容，再点击导航栏的标题按钮，选择几级标题。要三个步骤。而Markdown只需要在标题内容前加#即可</p>
<p>缺点：<br>
1、需要记一些语法（当然，是很简单。五分钟学会）。<br>
2、有些平台不支持Markdown编辑模式。</p>
<h1 id="一-标题">一、标题</h1>
<pre><code># 这是一级标题
## 这是二级标题
### 这是三级标题
#### 这是四级标题
##### 这是五级标题
###### 这是六级标题
</code></pre>
<h1 id="二-字体">二、字体</h1>
<pre><code>**这是加粗的文字**
*这是倾斜的文字*`
***这是斜体加粗的文字***
~~这是加删除线的文字
</code></pre>
<h1 id="三-引用">三、引用</h1>
<blockquote>
<p>这是引用的内容</p>
</blockquote>
<pre><code>&gt;这是引用的内容
</code></pre>
<h1 id="四-分割线">四、分割线</h1>
<hr>
<pre><code>---
</code></pre>
<h1 id="五-超链接">五、超链接</h1>
<p><a href="http://baidu.com">百度</a></p>
<pre><code>[百度](http://baidu.com)
</code></pre>
<h1 id="六-列表">六、列表</h1>
<ul>
<li>
<p>无序列表</p>
<p>-列表内容<br>
+列表内容<br>
*列表内容<br>
注意：- + * 跟内容之间都要有一个空格</p>
</li>
<li>
<p>有序列表</p>
<p>1.列表内容<br>
2.列表内容<br>
3.列表内容<br>
注意：序号跟内容之间要有空格</p>
</li>
</ul>
<h1 id="七-表格">七、表格</h1>
<pre><code>姓名|技能|排行
--|:--:|--:
刘备|哭|大哥
关羽|打|二哥
张飞|骂|三弟

表头|表头|表头
---|:--:|---:
内容|内容|内容
内容|内容|内容

第二行分割表头和内容。
有一个就行，为了对齐，多加了几个
文字默认居左
-两边加：表示文字居中
-右边加：表示文字居右
</code></pre>
<table>
<thead>
<tr>
<th>姓名</th>
<th style="text-align:center">技能</th>
<th style="text-align:right">排行</th>
</tr>
</thead>
<tbody>
<tr>
<td>刘备</td>
<td style="text-align:center">哭</td>
<td style="text-align:right">大哥</td>
</tr>
<tr>
<td>关羽</td>
<td style="text-align:center">打</td>
<td style="text-align:right">二哥</td>
</tr>
<tr>
<td>张飞</td>
<td style="text-align:center">骂</td>
<td style="text-align:right">三弟</td>
</tr>
</tbody>
</table>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[node.js笔记]]></title>
        <id>https://merry-9001.github.io/post/nodejs</id>
        <link href="https://merry-9001.github.io/post/nodejs">
        </link>
        <updated>2019-11-08T12:33:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="nodejs">node.js</h1>
<ul>
<li>node.js没有dom和bom操作</li>
<li>Node.js 是什么
<ul>
<li>JavaScript 运行时</li>
<li>既不是语言，也不是框架，它是一个平台</li>
</ul>
</li>
<li>Node.js 中的 JavaScript
<ul>
<li>没有 BOM、DOM</li>
<li>EcmaScript 基本的 JavaScript 语言部分</li>
<li>在 Node 中为 JavaScript 提供了一些服务器级别的 API
<ul>
<li>文件操作的能力</li>
<li>http 服务的能力</li>
</ul>
</li>
</ul>
</li>
</ul>
<p></p>
<h2 id="使用-require-方法加载-fs-核心模块">使用 require 方法加载 fs 核心模块</h2>
<p>fs 是 file-system 的简写，就是文件系统的意思</p>
<pre><code>var fs = require('fs')
</code></pre>
<h2 id="读取文件">读取文件</h2>
<p>第一个参数就是要读取的文件路径<br>
第二个参数是一个回调函数</p>
<p>成功<br>
data 数据<br>
error null</p>
<p>失败<br>
data undefined没有数据<br>
error 错误对象</p>
<pre><code>fs.readFile('./data/a.txt', function (error, data) {

if (error) {
    console.log('读取文件失败了')
} else {
    console.log(data.toString())
}
})
</code></pre>
<h2 id="创建web服务器">创建web服务器</h2>
<p>1.加载 http 核心模块</p>
<pre><code>var http = require('http')
</code></pre>
<p>2.使用 http.createServer() 方法创建一个 Web 服务器<br>
返回一个 Server 实例</p>
<pre><code>var server = http.createServer()
</code></pre>
<p>3.服务器操作</p>
<pre><code>server.on('request', function () {
console.log('收到客户端的请求了')
})
</code></pre>
<p>4.绑定端口号，启动服务器</p>
<pre><code>server.listen(3000, function () {
console.log('服务器启动成功了，可以通过 http://127.0.0.1:3000/ 来进行访问')
})
</code></pre>
<h2 id="服务器接收参数">服务器接收参数</h2>
<p>Request 请求对象<br>
请求对象可以用来获取客户端的一些请求信息，例如请求路径<br>
Response 响应对象<br>
响应对象可以用来给客户端发送响应消息</p>
<h2 id="服务器操作">服务器操作</h2>
<p>在 http 协议中，Content-Type 就是用来告知对方我给你发送的数据内容是什么类型</p>
<pre><code>var url = req.url

if (url === '/plain') {
    // text/plain 就是普通文本
    res.setHeader('Content-Type', 'text/plain; charset=utf-8')
    res.end('hello 世界')
} else if (url === '/html') {
    // 如果你发送的是 html 格式的字符串，则也要告诉浏览器我给你发送是 text/html 格式的内容
    res.setHeader('Content-Type', 'text/html; charset=utf-8')
    res.end('&lt;p&gt;hello html &lt;a href=&quot;&quot;&gt;点我&lt;/a&gt;&lt;/p&gt;')
}
</code></pre>
<p>可以采用读取图片的方式，然后如果文件读取成功的话,end返回一个（data），data就是数据</p>
<pre><code>fs.readFile('./resource/ab2.jpg', function (err, data) {
  if (err) {
    res.setHeader('Content-Type', 'text/plain; charset=utf-8')
    res.end('文件读取失败，请稍后重试！')
  } else {
    // data 默认是二进制数据，可以通过 .toString 转为咱们能识别的字符串
    // res.end() 支持两种数据类型，一种是二进制，一种是字符串
    // 图片就不需要指定编码了，因为我们常说的编码一般指的是：字符编码
    res.setHeader('Content-Type', 'image/jpeg')
    res.end(data)
  }
})
</code></pre>
<h2 id="一个json数据类型接口">一个json数据类型接口</h2>
<pre><code>    var products = [{
        name: '苹果 X',
        price: 8888
    },
    {
        name: '菠萝 X',
        price: 5000
    },
    {
        name: '小辣椒 X',
        price: 1999
    }
    ]

    // 响应内容只能是二进制数据或者字符串
    //  数字
    //  对象
    //  数组
    //  布尔值
    res.end(JSON.stringify(products))
</code></pre>
<h2 id="nodejs核心模块">node.js核心模块</h2>
<p>用来获取机器信息的</p>
<pre><code>var os = require('os')
</code></pre>
<p>用来操作路径的</p>
<pre><code>var path = require('path')
</code></pre>
<p>获取当前机器的 CPU 信息</p>
<pre><code>console.log(os.cpus())
</code></pre>
<p>memory 内存</p>
<pre><code>console.log(os.totalmem())
</code></pre>
<p>获取一个路径中的扩展名部分</p>
<pre><code>extname extension name
console.log(path.extname('c:/a/b/c/d/hello.txt'))
</code></pre>
<h2 id="nodejs理论细节">node.js理论细节</h2>
<p>在 Node 中没有全局作用域的概念<br>
在 Node 中，只能通过 require 方法来加载执行多个 JavaScript 脚本文件<br>
require 加载只能是执行其中的代码，文件与文件之间由于是模块作用域，所以不会有污染的问题</p>
<ul>
<li>模块完全是封闭的</li>
<li>外部无法访问内部</li>
<li>内部也无法访问外部<br>
模块作用域固然带来了一些好处，可以加载执行多个文件，可以完全避免变量命名冲突污染的问题</li>
</ul>
<ul>
<li>但是某些情况下，模块与模块是需要进行通信的</li>
<li>在每个模块中，都提供了一个对象：<code>exports</code></li>
<li>该对象默认是一个空对象</li>
<li>你要做的就是把需要被外部访问使用的成员手动的挂载到 <code>exports</code> 接口对象中</li>
<li>然后谁来 <code>require</code> 这个模块，谁就可以得到模块内部的 <code>exports</code> 接口对象</li>
</ul>
<h2 id="核心模块">核心模块</h2>
<p>核心模块是由 Node 提供的一个个的具名的模块，它们都有自己特殊的名称标识，例如</p>
<ul>
<li>fs 文件操作模块</li>
<li>http 网络服务构建模块</li>
<li>os 操作系统信息模块</li>
<li>path 路径处理模块</li>
</ul>
<h2 id="http">http</h2>
<ul>
<li>Content-Type
<ul>
<li>服务器最好把每次响应的数据是什么内容类型都告诉客户端，而且要正确的告诉</li>
<li>不同的资源对应的 Content-Type 是不一样，具体参照：http://tool.oschina.net/commons</li>
<li>对于文本类型的数据，最好都加上编码，目的是为了防止中文解析乱码问题</li>
</ul>
</li>
<li>通过网络发送文件
<ul>
<li>发送的并不是文件，本质上来讲发送是文件的内容</li>
<li>当浏览器收到服务器响应内容之后，就会根据你的 Content-Type 进行对应的解析处理</li>
</ul>
</li>
</ul>
<h2 id="关于js代码是否使用分号">关于js代码是否使用分号</h2>
<p>建议如果一行代码是以 (、[、` 开头的，则最好都在其前面补上一个分号。</p>
<h2 id="art-template模板">art-template模板</h2>
<ol>
<li>安装 npm install art-template</li>
<li>在需要使用的文件模块中加载 art-template<br>
只需要使用 require 方法加载就可以了：require('art-template')<br>
参数中的 art-template 就是你下载的包的名字<br>
也就是说你 isntall 的名字是什么，则你 require 中的就是什么</li>
<li>查文档，使用模板引擎的 API</li>
</ol>
<h2 id="服务端渲染和客户端渲染的区别">服务端渲染和客户端渲染的区别</h2>
<ul>
<li>客户端渲染不利于 SEO 搜索引擎优化</li>
<li>服务端渲染是可以被爬虫抓取到的，客户端异步渲染是很难被爬虫抓取到的</li>
<li>所以你会发现真正的网站既不是纯异步也不是纯服务端渲染出来的</li>
<li>而是两者结合来做的</li>
<li>例如京东的商品列表就采用的是服务端渲染，目的了为了 SEO 搜索引擎优化</li>
<li>而它的商品评论列表为了用户体验，而且也不需要 SEO 优化，所以采用是客户端渲染</li>
</ul>
<h2 id="moduleexports和exports">module.exports和exports</h2>
<p>谁 require 我，谁就得到 module.exports<br>
默认在代码的最后有一句：<br>
一定要记住，最后 return 的是 module.exports，而不是 exports<br>
可以只使用module.exports</p>
<h2 id="moduleexports-和-exports-的区别">module.exports 和 exports 的区别</h2>
<ul>
<li>每个模块中都有一个 module 对象</li>
<li>module 对象中有一个 exports 对象</li>
<li>我们可以把需要导出的成员都挂载到 module.exports 接口对象中</li>
<li>也就是：<code>moudle.exports.xxx = xxx</code> 的方式</li>
<li>但是每次都 <code>moudle.exports.xxx = xxx</code> 很麻烦，点儿的太多了</li>
<li>所以 Node 为了你方便，同时在每一个模块中都提供了一个成员叫：<code>exports</code></li>
<li><code>exports === module.exports</code> 结果为  <code>true</code>s</li>
<li>所以对于：<code>moudle.exports.xxx = xxx</code> 的方式 完全可以：<code>expots.xxx = xxx</code></li>
<li>当一个模块需要导出单个成员的时候，这个时候必须使用：<code>module.exports = xxx</code> 的方式</li>
<li>不要使用 <code>exports = xxx</code> 不管用</li>
<li>因为每个模块最终向外 <code>return</code> 的是 <code>module.exports</code></li>
<li>而 <code>exports</code> 只是 <code>module.exports</code> 的一个引用</li>
<li>所以即便你为 <code>exports = xx</code> 重新赋值，也不会影响 <code>module.exports</code></li>
<li>但是有一种赋值方式比较特殊：<code>exports = module.exports</code> 这个用来重新建立引用关系的</li>
</ul>
<hr>
<ul>
<li>exports 只是 module.exports 的一个引用而已，目的只是为了简化写法</li>
<li>每个模块最终 return 的是 module.exports</li>
</ul>
<h2 id="加载模块">加载模块</h2>
<ul>
<li>
<p>路径形式的模块：<br>
./ 当前目录，不可省略<br>
../ 上一级目录，不可省略</p>
</li>
<li>
<p>核心模块：<br>
核心模块的本质也是文件<br>
核心模块文件已经被编译到了二进制文件中了，我们只需要按照名字来加载就可以了<br>
require('fs')<br>
require('http')</p>
</li>
<li>
<p>第三方模块<br>
凡是第三方模块都必须通过 npm 来下载<br>
使用的时候就可以通过 require('包名') 的方式来进行加载才可以使用<br>
不可能有任何一个第三方包和核心模块的名字是一样的<br>
既不是核心模块、也不是路径形式的模块</p>
</li>
</ul>
<h1 id="express">Express</h1>
<h2 id="介绍">介绍</h2>
<ul>
<li>第三方 Web 开发框架</li>
<li>高度封装了 http 模块</li>
<li>更加专注于业务，而非底层细节</li>
<li>知其所以然</li>
</ul>
<ul>
<li>增删改查
<ul>
<li>使用文件来保存数据（锻炼异步编码）</li>
</ul>
</li>
<li>MongoDB
<ul>
<li>（所有方法都封装好了）</li>
</ul>
</li>
</ul>
<h2 id="使用">使用</h2>
<ol>
<li>
<p>安装npm</p>
</li>
<li>
<p>引包</p>
<p>var express = require('express')</p>
</li>
<li>
<p>创建你服务器应用程序</p>
<p>var app = express()</p>
</li>
</ol>
<h3 id="开放资源">开放资源</h3>
<p>只要这样做了，你就可以直接通过 /public/xx 的方式访问 public 目录中的所有资源了</p>
<pre><code>app.use('/public/', express.static('./public/'))
</code></pre>
<p>用户访问public地址的时候，给他显示public接口</p>
<h3 id="get请求">get请求</h3>
<pre><code>app.get('/about', function (req, res) {
// 在 Express 中可以直接 req.query 来获取查询字符串参数
console.log(req.query)
res.send('你好，我是 Express!')
})
</code></pre>
<h3 id="开启接口">开启接口</h3>
<p>相当于 server.listen</p>
<pre><code>app.listen(3000, function () {
console.log('app is running at port 3000.')
})
</code></pre>
<h2 id="使用模板">使用模板</h2>
<ol>
<li>
<p>安装express-art-template和art-template</p>
</li>
<li>
<p>配置 html表示使用的是html结尾的模板才能使用</p>
<p>app.engine('html', require('express-art-template'))</p>
<figure data-type="image" tabindex="1"><img src="https://merry-9001.github.io/post-images/1576909355217.png" alt="" loading="lazy"></figure>
</li>
</ol>
<h2 id="获取表单内容">获取表单内容</h2>
<ol>
<li>
<p>安装body-parser</p>
</li>
<li>
<p>配置</p>
<p>var bodyParser = require('body-parser')<br>
// 配置模板引擎和 body-parser 一定要在 app.use(router) 挂载路由之前<br>
// parse application/x-www-form-urlencoded<br>
app.use(bodyParser.urlencoded({ extended: false }))<br>
// parse application/json<br>
app.use(bodyParser.json())</p>
</li>
</ol>
<h1 id="回调函数">回调函数</h1>
<h2 id="如果需要获取一个函数中异步操作的结果则必须通过回调函数来获取">如果需要获取一个函数中异步操作的结果，则必须通过回调函数来获取</h2>
<pre><code>function fn(callback) {
    setTimeout(function() {
        var data = 'hello'
        callback(data)
    }, 1000)
}
fn(function(data) {
    console.log(data)
})
</code></pre>
<p>如果想取到data的数据进行处理，就只能进行回调函数，因为定时器是异步的，函数不会等待执行完毕就直接结束了，只有在调用的时候传递一个函数给里面的定时器才行</p>
<h2 id="异步">异步</h2>
<h2 id="一般情况下把函数作为参数的目的就是为了获取函数内部的异步操作结果javascript-单线程-事件循环">一般情况下，把函数作为参数的目的就是为了获取函数内部的异步操作结果<br>
JavaScript 单线程、事件循环</h2>
<p>异步操作的函数</p>
<ul>
<li>setTimeout</li>
<li>readFile</li>
<li>writeFile</li>
<li>ajax</li>
</ul>
<h2 id="promise">Promise</h2>
<p><img src="https://merry-9001.github.io/post-images/1576911008130.png" alt="" loading="lazy"><br>
<img src="https://merry-9001.github.io/post-images/1576911012957.png" alt="" loading="lazy"></p>
<h3 id="特点">特点</h3>
<h4 id="对象的状态不受外界影响-3种状态">对象的状态不受外界影响 （3种状态）</h4>
<ul>
<li>Pending状态（进行中）</li>
<li>Fulfilled状态（已成功）</li>
<li>Rejected状态（已失败）</li>
</ul>
<h4 id="一旦状态改变就不会再变-两种状态改变成功或失败">一旦状态改变就不会再变 （两种状态改变：成功或失败）</h4>
<ul>
<li>Pending -&gt; Fulfilled</li>
<li>Pending -&gt; Rejected</li>
</ul>
<h3 id="例子">例子</h3>
<pre><code>var p1 = new Promise(function (resolve, reject) {
fs.readFile('./data/a.txt', 'utf8', function (err, data) {
    if (err) {
    reject(err)
    } else {
    resolve(data)
    }
})
})

var p2 = new Promise(function (resolve, reject) {
fs.readFile('./data/b.txt', 'utf8', function (err, data) {
    if (err) {
    reject(err)
    } else {
    resolve(data)
    }
})
})

var p3 = new Promise(function (resolve, reject) {
fs.readFile('./data/c.txt', 'utf8', function (err, data) {
    if (err) {
    reject(err)
    } else {
    resolve(data)
    }
})
})

p1
.then(function (data) {
    console.log(data)
    return p2
}, function (err) {
    console.log('读取文件失败了', err)
})
.then(function (data) {
    console.log(data)
    return p3
})
.then(function (data) {
    console.log(data)
    console.log('end')
})
</code></pre>
<h2 id="当-p1-读取成功的时候当前函数中-return-的结果就可以在后面的-then-中-function-接收到当你-return-123-后面就接收到-123">当 p1 读取成功的时候<br>
当前函数中 return 的结果就可以在后面的 then 中 function 接收到<br>
当你 return 123 后面就接收到 123</h2>
<p>我们可以 return 一个 Promise 对象<br>
当 return 一个 Promise 对象的时候，后续的 then 中的 方法的第一个参数会作为 p2 的 resolve</p>
<h3 id="我觉得利用promise之后就不用层层嵌套了而且执行的顺序依旧是按照代码的顺序来执行不会乱">我觉得利用promise之后就不用层层嵌套了，而且执行的顺序依旧是按照代码的顺序来执行，不会乱</h3>
<pre><code>function pReadFile(filePath) {
    return new Promise(function (resolve, reject) {
        fs.readFile(filePath, 'utf8', function (err, data) {
        if (err) {
            reject(err)
        } else {
            resolve(data)
        }
        })
    })
}

pReadFile('./data/a.txt')
.then(function (data) {
    console.log(data)
    return pReadFile('./data/b.txt')
})
.then(function (data) {
    console.log(data)
    return pReadFile('./data/c.txt')
})
.then(function (data) {
    console.log(data)
})
</code></pre>
<h1 id="__dirname-和-__filename">__dirname 和 __filename</h1>
<ul>
<li>动态的获取当前文件或者文件所处目录的绝对路径</li>
<li>用来解决文件操作路劲的相对路径问题</li>
<li>因为在文件操作中，相对路径相对于执行 <code>node</code> 命令所处的目录</li>
<li>所以为了尽量避免这个问题，都建议文件操作的相对路劲都转为：<strong>动态的绝对路径</strong></li>
<li>方式：<code>path.join(__dirname, '文件名')</code></li>
</ul>
<p>用了这个他就会帮助你获取当前的电脑的绝对路径，避免了相对路径会遇到的问题也不至于会写死<br>
__dirname是用来获取文件的目录<br>
__filename可以用来获取当前的文件名<br>
就是专门用来动态的获取当前文件以及文件所属目录的绝对路径</p>
<h1 id="mysql">Mysql</h1>
<h2 id="例子-2">例子</h2>
<p>先导包(npm)<br>
再使用<br>
var mysql = require('mysql');</p>
<pre><code>// 1. 创建连接
var connection = mysql.createConnection({
host: 'localhost',
user: 'root',
password: 'root',
database: 'users' // 对不起，我一不小心把数据库名字和表名起成一样的，你知道就行
});

// 2. 连接数据库 打开冰箱门
connection.connect();

// 3. 执行数据操作 把大象放到冰箱
connection.query('SELECT * FROM `users`', function (error, results, fields) {
if (error) throw error;
console.log('The solution is: ', results);
});

// connection.query('INSERT INTO users VALUES(NULL, &quot;admin&quot;, &quot;123456&quot;)', function (error, results, fields) {
//   if (error) throw error;
//   console.log('The solution is: ', results);
// });

// 4. 关闭连接 关闭冰箱门
connection.end();
</code></pre>
<h1 id="mongodb-数据库">MongoDB 数据库</h1>
<h2 id="mongodb-的数据存储结构">MongoDB 的数据存储结构</h2>
<ul>
<li>数据库</li>
<li>集合（表）</li>
<li>文档（表记录）</li>
</ul>
<h2 id="mongodb-官方有一个-mongodb-的包可以用来操作-mongodb-数据库">MongoDB 官方有一个 mongodb 的包可以用来操作 MongoDB 数据库</h2>
<ul>
<li>这个确实和强大，但是比较原始，麻烦，所以咱们不使用它</li>
</ul>
<h2 id="mongoose">mongoose</h2>
<ul>
<li>真正在公司进行开发，使用的是 mongoose 这个第三方包</li>
<li>它是基于 MongoDB 官方的 mongodb 包进一步做了封装</li>
<li>可以提高开发效率</li>
<li>让你操作 MongoDB 数据库更方便</li>
</ul>
<h2 id="安装mongodb数据库">安装mongodb数据库</h2>
<ol>
<li>
<p>解压文件</p>
</li>
<li>
<p>配置环境变量<br>
<img src="https://merry-9001.github.io/post-images/1576913007511.png" alt="" loading="lazy"></p>
<p>C:\Users\six\Downloads\mongodb\mongodb-win32-x86_64-2008plus-ssl-4.0.3\bin</p>
</li>
<li>
<p>cmd里面输入 mongo -version测试</p>
</li>
</ol>
<h2 id="启动mongodb数据库">启动mongodb数据库</h2>
<pre><code>cmd中输入mongod(cmd1)
mongo show dbs(cmd2)
</code></pre>
<h2 id="mongoose包">mongoose包</h2>
<ol>
<li>
<p>安装</p>
</li>
<li>
<p>引入</p>
<p>var mongoose = require('mongoose')</p>
</li>
</ol>
<h3 id="例子-3">例子</h3>
<pre><code>var mongoose = require('mongoose')

// 连接数据库
mongoose.connect('mongodb://localhost/test', { useMongoClient: true })

var Schema = mongoose.Schema

var userSchema = new Schema({
email: {
    type: String,
    required: true
},
nickname: {
    type: String,
    required: true
}
})

module.exports = mongoose.model('User', userSchema)
</code></pre>
<p>只需要执行代码就会创建使用，如果存在的话就可以直接用.<br>
localhost/表名</p>
<h2 id="操作数据库">操作数据库</h2>
<h3 id="mongodb数据库自己会创建一个主键唯一标识">mongodb数据库自己会创建一个主键（唯一标识）</h3>
<h3 id="select-了解一下">select * 了解一下</h3>
<pre><code>Student.find(function(err, students) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.render('index.html', {
        fruits: [
            '苹果',
            '香蕉',
            '橘子'
        ],
        students: students
    })
})
</code></pre>
<h3 id="条件查询">条件查询</h3>
<pre><code>var body = req.body

User.findOne({
    email: body.email,
    password: md5(md5(body.password))
}, function (err, user) {
    if (err) {
    return res.status(500).json({
        err_code: 500,
        message: err.message
    })
}

{
    $or: [{
        email: body.email
    },
    {
        nickname: body.nickname
    }
    ]
}
</code></pre>
<p>or条件与and条件</p>
<h3 id="插入">插入</h3>
<pre><code>new Student(req.body).save(function(err) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.redirect('/students')
})
</code></pre>
<h3 id="查询一条记录">查询一条记录</h3>
<pre><code>Student.findById(req.query.id.replace(/&quot;/g, ''), function(err, student) {
    if (err) {
        console.log(err)
        return res.status(500).send('Server error.')
    }
    res.render('edit.html', {
        student: student
    })
})
</code></pre>
<h3 id="更新数据">更新数据</h3>
<pre><code>var id = req.body.id.replace(/&quot;/g, '')
Student.findByIdAndUpdate(id, req.body, function(err) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.redirect('/students')
})
</code></pre>
<h3 id="删除数据">删除数据</h3>
<pre><code>var id = req.query.id.replace(/&quot;/g, '')
Student.findByIdAndRemove(id, function(err) {
    if (err) {
        return res.status(500).send('Server error.')
    }
    res.redirect('/students')
})        
</code></pre>
<p>查找一条数据，第一个参数传条件，第二参数是一个函数，用来执行回调方法</p>
<h1 id="session">session</h1>
<h3 id="装包引用包">装包，引用包</h3>
<pre><code>var session = require('express-session')
</code></pre>
<h3 id="配置">配置</h3>
<pre><code>app.use(session({
    // 配置加密字符串，它会在原有加密基础之上和这个字符串拼起来去加密
    // 目的是为了增加安全性，防止客户端恶意伪造
    secret: 'itcast',
    resave: false,
    saveUninitialized: false // 无论你是否使用 Session ，我都默认直接给你分配一把钥匙
}))
</code></pre>
<h3 id="使用-2">使用</h3>
<pre><code>当把这个插件配置好之后，我们就可以通过 req.session 来发访问和设置 Session 成员了
添加 Session 数据：req.session.foo = 'bar'
访问 Session 数据：req.session.foo
</code></pre>
<h3 id="例子-4">例子</h3>
<pre><code>res.render('index.html', {
    user: req.session.user
})

req.session.user = user

// 清除登陆状态
req.session.user = null
</code></pre>
<h1 id="packagejson">package.json</h1>
<h2 id="介绍-2">介绍</h2>
<ul>
<li>就是产品的说明书</li>
<li><code>dependencies</code> 属性，用来保存项目的第三方包依赖项信息</li>
<li>所以建议每个项目都要有且只有一个 package.json (存放在项目的根目录)</li>
<li>我们可以通过 <code>npm init [--yes]</code> 来生成 package.json 文件</li>
<li>同样的，为了保存依赖项信息，我们每次安装第三方包的时候都要加上：<code>--save</code> 选项。</li>
</ul>
<p>在文件夹cmd中输入npm init，获取package.json文件<br>
然后你使用npm装包就可以看见，package.json-lock可以帮助锁定版本，也可以提高速速</p>
<h1 id="模板继承">模板继承</h1>
<h2 id="art-template例子">art-template例子</h2>
<p>layout.html<br>
<img src="https://merry-9001.github.io/post-images/1576921668859.png" alt="" loading="lazy"></p>
<p>index.html<br>
<img src="https://merry-9001.github.io/post-images/1576921676633.png" alt="" loading="lazy"></p>
<p>这样使用以后，加载index.html的时候就可以自动先去找layout，非常好用，<strong>方便复用</strong></p>
<h1 id="中间件">中间件</h1>
<h3 id="中间件处理请求的本质就是个函数">中间件：处理请求的，本质就是个函数</h3>
<p>当请求进来，会从第一个中间件开始进行匹配</p>
<ul>
<li>如果匹配，则进来
<ul>
<li>如果请求进入中间件之后，没有调用 next 则代码会停在当前中间件</li>
<li>如果调用了 next 则继续向后找到第一个匹配的中间件</li>
</ul>
</li>
<li>如果不匹配，则继续判断匹配下一个中间件</li>
</ul>
<p><strong>不关心请求路径和请求方法的中间件</strong></p>
<p><strong>也就是说任何请求都会进入这个中间件</strong></p>
<p>中间件本身是一个方法，该方法接收三个参数：<br>
Request 请求对象<br>
Response 响应对象<br>
next     下一个中间件</p>
<p>通配</p>
<pre><code>app.use(function (req, res, next) {
console.log(1)
next()
})
</code></pre>
<p>以 /a 开头的路径中间件</p>
<pre><code>app.use('/a', function (req, res, next) {
console.log('a')
next()
})
</code></pre>
<p>精准匹配<br>
app.get('/abc', function(req, res, next) {<br>
console.log('abc')<br>
next()<br>
})</p>
<h3><font color="red">如果不写next()就不会继续执行下个中间件
其实之前用的很多插件使用use的配置都是采用了中间件，然后next()到下一个
所以才能使用那些方法
</font></h3>
<p>配置错误处理中间件</p>
<pre><code>next(err)
app.use(function (err, req, res, next) {
    es.status(500).send(err.message)
})
</code></pre>
<p>如果使用中间的next()里面传递了参数，那么就会直接进入错误处理中间件，这个中间件必须拥有四个参数</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[毕业设计文献]]></title>
        <id>https://merry-9001.github.io/post/bi-ye-she-ji-wen-xian</id>
        <link href="https://merry-9001.github.io/post/bi-ye-she-ji-wen-xian">
        </link>
        <updated>2019-11-08T12:32:04.000Z</updated>
        <content type="html"><![CDATA[<p>基于PHP技术的校园综合服务网站的建设<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=JYJU201844038&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEWFBUZXJFRFIxSmYzbi9ydz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDM2OTkxRnJDVVJMT2VaK1JvRnl2Z1ZyektMelRCZTdHNEg5bklxNDlHYklSOGVYMUx1eFlTN0RoMVQzcVRyV00=</p>
<p>毕业季私人定制服务营销策略浅析<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=ZQGZ201806023&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEWE5kK3FTK09mL2JsMXNibz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjM0OThNMUZyQ1VSTE9lWitSb0Z5dmhVNy9QUHp6TWRMRzRIOW5NcVk5SFo0UjhlWDFMdXhZUzdEaDFUM3FUclc=</p>
<p>基于Liferay平台的私人订制功能及应用<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CMFD&amp;dbname=CMFD201702&amp;filename=1017193354.nh&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEV3RzN3ZsRlFFQjQraUdDRT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjIwNDIxTHV4WVM3RGgxVDNxVHJXTTFGckNVUkxPZVorUm9GeXZoVWIvQlZGMjZHYkt4SGRMSnE1RWJQSVI4ZVg=</p>
<p>[1]杨嘉鑫.校园最后一公里配送服务研究——以A大学城为例[J].价值工程,2019,38(24):232-233.<br>
校园最后一公里配送服务研究——以A大学城为例<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2019&amp;filename=JZGC201924097&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEV3RzN3ZsRlFFQjQraUdDRT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MTk4MjFYMUx1eFlTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnl2aFY3L0xMemZNYmJHNEg5ak9xNDlNWTRSOGU=</p>
<p>基于java语言的校园服务平台设计<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2017&amp;filename=RJDJ201710035&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVEV3N1eXNhaUdRbkovOFFQVT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDkwMzJUM3FUcldNMUZyQ1VSTE9lWitSb0Z5cmxVN3JJTnlmUFpMRzRIOWJOcjQ5R1lZUjhlWDFMdXhZUzdEaDE=</p>
<p>基于Vue.js框架的掌上零售系统的设计与实现<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CMFD&amp;dbname=CMFD201901&amp;filename=1019601282.nh&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFRk1DMU5tbHY3NEVEemhDYz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjY2MjFyQ1VSTE9lWitSb0Z5cmxWNy9KVkYyNkY3VzRIOVBFclpFYlBJUjhlWDFMdXhZUzdEaDFUM3FUcldNMUY=</p>
<p>一种校园生活一站式服务平台的设计与实现<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2019&amp;filename=XXDL201910026&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFRk1DMU5tbHY3NEVEemhDYz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MjI1MDk5ak5yNDlIWW9SOGVYMUx1eFlTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnlybVU3N05QVFhQWXJHNEg=</p>
<p>智慧校园移动系统数据库设计<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=AYSF201802015&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFR2paYklwblFIQjhUcEh2UT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDA5Njk5bk1yWTlFWVlSOGVYMUx1eFlTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnlybVVMckpKRFRZYUxHNEg=</p>
<p>校园网二级网站建设数据库设计<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2016&amp;filename=DZRU201609156&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVFR2psdVFVUVNwM3lVUk5lND0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDkzOTBoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnlybVViM0JJVGZaZTdHNEg5Zk1wbzVBWW9SOGVYMUx1eFlTN0Q=</p>
<p>基于HTML5响应式页面的研究与应用<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2018&amp;filename=NMKJ201818031&amp;uid=WEEvREdxOWJmbC9oM1NjYkZCbDdrNXcxeGc0Mjd1bU1iTDMwSCtLeEhGVXM=$R1yZ0H6jyaa0en3RxVUd8df-oHi7XMMDo7mtKT6mSmEvTuk11l2gFA!!&amp;v=MDczNDdTN0RoMVQzcVRyV00xRnJDVVJMT2VaK1JvRnk3aFY3ek9LeURBWkxHNEg5bk5wNDlHWllSOGVYMUx1eFk=</p>
<p>基于Vue.js的后台单页应用管理系统的研究与实现<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2017&amp;filename=XDJS201730011&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVVVVFQb0tLMWpjNnpNTUMrTT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MTMxMzlLUFNuQmZiRzRIOWJQcjQ5RVpZUjhlWDFMdXhZUzdEaDFUM3FUcldNMUZyQ1VSTE9lWitSb0Z5N2hXcjc=</p>
<p>纸媒突围:提供订制服务<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDLAST2015&amp;filename=PXWS201412009&amp;uid=WEEvREcwSlJHSldRa1FhdkJkVG5heFhtWnVVVVFQb0tLMWpjNnpNTUMrTT0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MDg3OTkxRnJDVVJMT2VaK1JvRnlIa1ZML09OVFhjZmJHNEg5WE5yWTlGYllSOGVYMUx1eFlTN0RoMVQzcVRyV00=</p>
<p>MySQL数据库备份及恢复方法解析<br>
https://kns.cnki.net/KCMS/detail/detail.aspx?dbcode=CJFQ&amp;dbname=CJFDTEMP&amp;filename=WLAQ201910038&amp;uid=WEEvREcwSlJHSldRa1Fhb09jT0lPd1RLUG54SzhxYUFTWlpDZ3Yrcm00cz0=$9A4hF_YAuvQ5obgVAqNKPCYcEjKensW4IQMovwHtwkF4VYPoHbKxJw!!&amp;v=MTYyNTJGeURoVkwvSU1pSEtmN0c0SDlqTnI0OUdiSVI4ZVgxTHV4WVM3RGgxVDNxVHJXTTFGckNVUkxPZVorUm8=</p>
<p>Using collaborative filtering to support college students’ hu2014</p>
<p>use of online forum for English learning wang2012</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[npm]]></title>
        <id>https://merry-9001.github.io/post/npm</id>
        <link href="https://merry-9001.github.io/post/npm">
        </link>
        <updated>2019-10-25T02:08:52.000Z</updated>
        <content type="html"><![CDATA[<p>下载node获得npm</p>
<p>可以安装nrm<br>
npm i nrm -g</p>
<p>样可以下包使用cnpm<br>
npm i cnpm -g这</p>
<p>可以查看<br>
nrm ls</p>
<p>可以使用cnpm管理<br>
nrm use npm</p>
<p>使用淘宝镜像的命令：</p>
<p>npm install -g cnpm --registry=https://registry.npm.taobao.org</p>
]]></content>
    </entry>
</feed>